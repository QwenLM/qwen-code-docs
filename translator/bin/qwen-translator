#!/usr/bin/env node

// 直接调用 src/cli.ts 中的主函数
const { main } = require("../dist/cli.js");

// 执行主函数
main().catch((error) => {
  console.error("❌ Program execution failed:", error.message);
  process.exit(1);
});
