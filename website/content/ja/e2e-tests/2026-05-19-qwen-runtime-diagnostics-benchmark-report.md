# Qwen Code Runtime Diagnostics ベンチマークレポート

日付: 2026-05-19

## スコープ

この実行では、新しいオプトインのランタイム診断を有効にして、以前の Qwen Code ベンチマークの形状を繰り返します。Qwen Code のみをテストし、Claude Code はテストしません。

初期モデルマトリックス:

- `pai/glm-5`
- `qwen3.6-plus`

追加の PR サイズフォローアップ:

- `DeepSeek/deepseek-v4-pro`（Anthropic 互換プロトコル経由）

ケース:

- 小さな GitHub PR レビュー: PR `#4268`
- コードナビゲーション: 圧縮 / コンパクション関連のコード検索と読み取り
- 合成ローカル diff: 約 94.6 KiB
- 合成ローカル diff: 約 968.5 KiB
- 合成ローカル diff: 約 4.84 MiB

この実行では、診断ブランチのローカルバンドル CLI を `QWEN_CODE_PROFILE_RUNTIME=1` と一時的な CLI ホームディレクトリで使用しました。このベンチマークでは、グローバル MCP サーバーとフックは読み込まれませんでした。

重要な注意点: これらの絶対 RSS 値は、以前の PATH 解決された `qwen` の実行よりも低くなっています。これは、この実行がローカルブランチの `node dist/cli.js` と、ストリップされた一時的な設定を使用したためです。このレポートは内部診断配布実行として扱い、以前のインストール済み CLI との直接的な RSS 比較の代替としないでください。

## インストール済み CLI vs ローカルバンドルの健全性チェック

フォローアップの健全性チェックでは、インストール済み CLI とローカル診断バンドルで、同じ最小限のプロンプト、モデル、非対話モードを使用しました。唯一の意図的な変数は、Qwen Code がストリップされた一時的な CLI ホームを読み込むか、通常のユーザー設定を読み込むかです。

| CLI                 | 設定モード     | トークン合計 | Tree RSS ピーク | Root RSS ピーク | プロセス数ピーク | ランタイム診断 |
| ------------------- | --------------- | -----------: | ------------: | ------------: | -----------------: | ------------------- |
| PATH `qwen`         | ストリップ設定 |       33,965 |     542.4 MiB |     249.9 MiB |                  3 | なし             |
| ローカル `dist/cli.js` | ストリップ設定 |       47,281 |     455.2 MiB |     214.2 MiB |                  4 | あり                |
| PATH `qwen`         | 通常設定   |       97,615 |   1,099.9 MiB |     250.1 MiB |                  6 | なし             |
| ローカル `dist/cli.js` | 通常設定   |       97,954 |   1,105.4 MiB |     212.7 MiB |                  8 | あり                |

このチェックにより、原因の特定が変わります: 以前の 1 GiB のユーザー可視ピークは、ローカル診断バンドルでも通常設定で再現可能です。したがって、これは主にローカルブランチに PR `#4186` が含まれていることによって説明されるわけではありません。

通常設定のピーク時、ローカルプロセスツリーサンプルは、Qwen ルートプロセス単独ではなく、複数の Node/MCP プロセスによって支配されていました:

| ロール  | コマンド形状             | ツリーピーク時の RSS |
| ----- | ------------------------- | ---------------: |
| 子プロセス | Node プロセス              |        252.9 MiB |
| 子プロセス | Chrome DevTools MCP       |        219.7 MiB |
| 子プロセス | Node プロセス              |        219.2 MiB |
| ルート  | Qwen Node プロセス         |        215.1 MiB |
| 子プロセス | Chrome DevTools MCP セットアップ |        175.2 MiB |

PR `#4186` はローカル診断ブランチに存在しますが、これは V8 ヒーププレッシャー自動コンパクションのセーフティネットです。V8 ヒーププレッシャーが約 70% でトリガーされます。この環境では Node ヒープ制限は約 4.1 GiB ですが、ストリップされたベンチマーク終了時のヒープは約 99-143 MiB でした。これらの数値に基づくと、ストリップ設定の RSS が低いのは、これらのベンチマーク実行中に `#4186` がコンテキストを積極的に圧縮したことが原因ではありません。

### ベアモード設定の原因特定チェック

2 番目のフォローアップでは、インストール済み CLI とローカルバンドルの両方で、同じ PR レビュープロンプト形状で `qwen3.6-plus` を使用しました。これは通常のエンドツーエンドのビジネスベンチマークではありません。起動/設定メモリのみを制御された原因特定チェックです。

`--bare` はランタイム入力を変更します。通常のグローバル設定の検出、MCP 起動、フック、暗黙のコンテキスト、スキル、その他の起動統合をスキップします。そのため、モデルプロバイダーがグローバル設定でのみ構成されている場合、失敗したり異なる動作をしたりする可能性があります。この実行では、モデル認証情報は子プロセス環境を介してのみ提供されました。ベアモードは意図的に通常のプロバイダー設定を読み込まないためです。ユーザーのグローバル設定には何も書き戻されませんでした。

この実行では有用なトークン/ツール呼び出し統計は生成されませんでした。モデルは 1 ターンで完了し、要求されたシェルコマンドを呼び出しませんでした。これらの行を通常のタスクベンチマーク結果として使用したり、上記のマトリックスとトークン/ツール呼び出し動作を比較したりしないでください。これらは、通常の設定と設定された子プロセスからプロセスツリー RSS がどの程度発生するかを見積もるためにのみ役立ちます。

| CLI                 | モード     | 壁時計 | ターン数 | ツール使用数 | Tree RSS ピーク | Root RSS ピーク | プロセス数ピーク |
| ------------------- | -------- | ---: | ----: | --------: | ------------: | ------------: | -----------------: |
| PATH `qwen`         | 通常   | 5.5s |     1 |         0 |   1,021.3 MiB |     251.5 MiB |                  5 |
| PATH `qwen`         | `--bare` | 2.4s |     1 |         0 |     525.7 MiB |     246.4 MiB |                  2 |
| ローカル `dist/cli.js` | 通常   | 4.9s |     1 |         0 |   1,046.2 MiB |     213.3 MiB |                  5 |
| ローカル `dist/cli.js` | `--bare` | 2.3s |     1 |         0 |     454.3 MiB |     216.5 MiB |                  3 |

結果は、起動/設定の原因特定に関するプロセスツリー仮説を確認しています。このマシンでは、通常設定により、`--bare` と比較してユーザー可視のプロセスツリー RSS が約 0.50-0.59 GiB 追加されますが、ルート RSS は同じ 0.21-0.25 GiB の範囲に留まります。通常設定のピーク時には、追加の RSS は再び追加の Node/MCP 子プロセス（Chrome DevTools MCP プロセスとそのセットアップラッパーを含む）から発生しました。`--bare` はこれらの起動/設定子プロセスを削除し、インストール済み/ローカル実行のツリー RSS を 0.45-0.53 GiB の範囲に戻します。

### 一時設定の MCP / フック分離

`--bare` はランタイム入力を多く変更するため、通常のベンチマークとして扱えません。そのため、フォローアップでは、通常設定から派生した生成設定ファイルを使用して、一時的な `QWEN_HOME` ディレクトリを使用しました。実行は通常の設定読み込みパスを維持しましたが、設定の 2 つの次元のみを切り替えました:

- MCP 無効: `mcpServers` をクリアし、MCP 許可/除外リストを空にしました。
- フック無効: `disableAllHooks` を true に設定。

グローバル設定は変更されていません。このケースでは `qwen3.6-plus` と最小限の起動プロンプトを使用したため、タスク推論品質ではなく、起動/設定のプロセスツリーコストを測定します。

| CLI                 | 一時設定     | MCP サーバー数 | ツール数 | Tree RSS ピーク | Root RSS ピーク | プロセス数ピーク |
| ------------------- | -------------------- | ----------: | ----: | ------------: | ------------: | -----------------: |
| PATH `qwen`         | 完全                 |           4 |    46 |   1,017.4 MiB |     249.8 MiB |                  5 |
| PATH `qwen`         | MCP 無効         |           0 |    17 |     548.7 MiB |     252.4 MiB |                  2 |
| PATH `qwen`         | フック無効       |           4 |    46 |   1,003.8 MiB |     246.4 MiB |                  5 |
| PATH `qwen`         | MCP + フック無効 |           0 |    17 |     542.5 MiB |     248.0 MiB |                  2 |
| ローカル `dist/cli.js` | 完全                 |           4 |    48 |     865.9 MiB |     220.4 MiB |                  6 |
| ローカル `dist/cli.js` | MCP 無効         |           0 |    19 |     442.9 MiB |     209.6 MiB |                  2 |
| ローカル `dist/cli.js` | フック無効       |           4 |    48 |     848.3 MiB |     212.6 MiB |                  5 |
| ローカル `dist/cli.js` | MCP + フック無効 |           0 |    19 |     447.2 MiB |     217.8 MiB |                  2 |

解釈:

1. MCP を無効にすることが支配的な変更です。これにより、4 つの MCP サーバーが削除され、アドバタイズされるツール数が約 29 減少し、この起動/設定ケースではプロセスツリー RSS が約 0.42-0.47 GiB 減少します。
2. フックのみを無効にしても、このケースでは RSS はほとんど変わりません。これは、プロンプトがツール呼び出しを生成しなかったため、`PreToolUse` / `PostToolUse` フックが実行されなかったため、予想通りです。
3. ルートプロセスはすべての行で約 0.21-0.25 GiB に留まっています。大きな違いは再びプロセスツリー構成であり、ルート Qwen RSS ではありません。

`qwen3.6-plus` と `pai/glm-5` を使用した 2 回のコードナビゲーションフォローアップも同じ MCP あり/なしのメモリ分割を再現しましたが、どちらのモデルもそれらの実行でツール呼び出しを生成しませんでした。したがって、これらの行はフック実行の証拠として使用されません。有効なフックベンチマークには、ツール呼び出しを確実に生成するタスク/モデルの組み合わせが依然として必要です。

### MCP ごとの分離

前の行では、MCP グループ全体が起動/設定メモリの支配的な要因であることが示されました。フォローアップでは、設定された各 MCP サーバーを分離し、すべての行でフックを無効にしたままにしました。これにより、テストは通常の設定読み込みパスに維持されますが、MCP サーバーのサブセットのみが変更されます。

設定された MCP サーバー名:

- `approval-bridge`
- `env-center`
- `chrome-devtools`
- `code`

シングルパス分離:

| バリアント                   | 有効な MCP                                       | ツール数 | MCP サーバー数 | Tree RSS ピーク | Root RSS ピーク | 解釈                               |
| ------------------------- | -------------------------------------------------- | ----: | ----------: | ------------: | ------------: | ------------------------------------ |
| なし                      | なし                                               |    19 |           0 |     444.4 MiB |     211.7 MiB | MCP なしのベースライン                 |
| 完全                      | 全 4 つ                                              |    48 |           4 |     857.3 MiB |     215.9 MiB | フル MCP 起動形状               |
| `approval-bridge` のみ    | `approval-bridge`                                  |    19 |           1 |     455.5 MiB |     214.0 MiB | ベースラインに近い                        |
| `env-center` のみ         | `env-center`                                       |    19 |           1 |     452.3 MiB |     214.4 MiB | ベースラインに近い                        |
| `chrome-devtools` のみ    | `chrome-devtools`                                  |    48 |           1 |     824.4 MiB |     209.5 MiB | 大きな RSS 増加とツール増加 |
| `code` のみ               | `code`                                             |    19 |           1 |     452.1 MiB |     216.6 MiB | ベースラインに近い                        |
| `approval-bridge` なし    | `env-center`, `chrome-devtools`, `code`            |    48 |           3 |     997.1 MiB |     215.4 MiB | 依然として高い; 実行にばらつきあり      |
| `env-center` なし         | `approval-bridge`, `chrome-devtools`, `code`       |    48 |           3 |     863.8 MiB |     220.9 MiB | 依然として高い                           |
| `chrome-devtools` なし    | `approval-bridge`, `env-center`, `code`            |    19 |           3 |     463.4 MiB |     221.6 MiB | ベースラインに近い戻る                |
| `code` なし               | `approval-bridge`, `env-center`, `chrome-devtools` |    48 |           3 |     858.1 MiB |     219.5 MiB | 依然として高い                           |

起動 RSS にはばらつきがあるため、主要なバリアントを 2 回繰り返しました:

| バリアント                   | サンプル数 | Tree RSS 範囲      | 平均 Tree RSS | 結果                         |
| ------------------------- | ------: | ------------------- | -----------: | ------------------------------ |
| なし                      |       2 | 443.3-451.9 MiB     |    447.6 MiB | 安定した MCP なしベースライン         |
| 完全                      |       2 | 856.1-922.8 MiB     |    889.5 MiB | 安定した高 MCP 範囲          |
| `chrome-devtools` のみ    |       2 | 1,007.1-1,021.2 MiB |  1,014.2 MiB | 単独で高 RSS を再現可能 |
| `chrome-devtools` なし    |       2 | 461.1-461.6 MiB     |    461.4 MiB | 高 RSS を除去           |
| `approval-bridge` のみ    |       2 | 449.1-449.9 MiB     |    449.5 MiB | ベースラインに近い                  |
| `env-center` のみ         |       2 | 438.7-449.5 MiB     |    444.1 MiB | ベースラインに近い                  |
| `code` のみ               |       2 | 450.6-451.3 MiB     |    451.0 MiB | ベースラインに近い                  |

解釈:

1. `chrome-devtools` は、この環境における支配的な MCP 貢献者です。これ単独で、高いプロセスツリー RSS を再現するのに十分です。
2. フル MCP セットから `chrome-devtools` を削除すると、RSS は MCP なし帯域に戻ります。`chrome-devtools` を維持しながら他の MCP を削除しても、そうはなりません。
3. アドバタイズされるツール数も同じパターンに従います: ベースラインは 19 ツールですが、`chrome-devtools` はツール数を 48 に増やします。つまり、この MCP はプロセスツリー RSS だけでなく、リクエストツールスキーマサイズとトークンプレッシャーも増加させる可能性が高いことを意味します。
4. `approval-bridge`、`env-center`、`code` は、これらの起動/設定実行では個別に MCP なしベースラインに近い状態を維持しました。これらの実行では起動警告が出力されたため、この結果は「すべてのワークフローでコストがゼロであることの証明ではなく、永続的な起動 RSS 所有者は観察されなかった」と解釈する必要があります。

## ランタイムサマリー

| ケース             | モデル          |  壁時計 | ターン数 | トークン合計 | Tree RSS ピーク | Root RSS ピーク |  終了時ヒープ |  終了時 RSS |
| ---------------- | -------------- | ----: | ----: | -----------: | ------------: | ------------: | --------: | --------: |
| 小さい PR `#4268` | `pai/glm-5`    | 20.1s |     7 |      173,216 |     362.1 MiB |     359.8 MiB | 103.1 MiB | 216.5 MiB |
| コードナビゲーション  | `pai/glm-5`    | 18.4s |     2 |       49,127 |     378.0 MiB |     376.0 MiB | 102.4 MiB | 313.4 MiB |
| diff 94.6 KiB    | `pai/glm-5`    | 16.6s |     6 |      135,716 |     367.9 MiB |     366.0 MiB |  99.1 MiB | 295.0 MiB |
| diff 968.5 KiB   | `pai/glm-5`    | 11.4s |     2 |       42,590 |     373.2 MiB |     362.5 MiB | 106.4 MiB | 345.6 MiB |
| diff 4.84 MiB    | `pai/glm-5`    | 12.0s |     4 |       95,119 |     414.2 MiB |     412.0 MiB | 123.6 MiB | 410.7 MiB |
| 小さい PR `#4268` | `qwen3.6-plus` | 35.0s |     6 |      156,556 |     358.9 MiB |     356.9 MiB | 102.6 MiB | 293.1 MiB |
| コードナビゲーション  | `qwen3.6-plus` | 28.9s |     4 |       99,800 |     370.3 MiB |     368.3 MiB | 105.8 MiB | 298.2 MiB |
| diff 94.6 KiB    | `qwen3.6-plus` | 28.3s |     4 |       90,808 |     358.8 MiB |     356.9 MiB | 105.9 MiB | 307.0 MiB |
| diff 968.5 KiB   | `qwen3.6-plus` | 30.9s |     6 |      151,782 |     366.1 MiB |     364.1 MiB | 101.0 MiB | 316.9 MiB |
| diff 4.84 MiB    | `qwen3.6-plus` | 24.1s |     4 |       93,271 |     372.8 MiB |     366.0 MiB | 142.8 MiB | 366.0 MiB |

モデル別平均:

| モデル          | 平均 Tree RSS ピーク | 平均 Root RSS ピーク | 平均ターン数 | 平均トークン合計 | 平均最大ワイヤボディ | 平均ツール結果合計 |
| -------------- | ----------------: | ----------------: | --------: | ---------------: | ----------------: | --------------------: |
| `pai/glm-5`    |         379.1 MiB |         375.3 MiB |       4.2 |           99,154 |         111.8 KiB |             335.1 KiB |
| `qwen3.6-plus` |         365.4 MiB |         362.4 MiB |       4.8 |          118,443 |         119.3 KiB |             344.3 KiB |

小さい PR `#4268` の重複モデルスナップショット:

| モデル                      | プロトコル  |  壁時計 | ターン数 | トークン合計 | Tree RSS ピーク | Root RSS ピーク | 最大ワイヤボディ |
| -------------------------- | --------- | ----: | ----: | -----------: | ------------: | ------------: | ------------: |
| `pai/glm-5`                | OpenAI    | 20.1s |     7 |      173,216 |     362.1 MiB |     359.8 MiB |     113.8 KiB |
| `qwen3.6-plus`             | OpenAI    | 35.0s |     6 |      156,556 |     358.9 MiB |     356.9 MiB |     134.1 KiB |
| `DeepSeek/deepseek-v4-pro` | Anthropic | 39.7s |     2 |       43,362 |     346.9 MiB |     344.8 MiB |     103.0 KiB |

## リクエストおよびツール診断

| ケース             | モデル          | リクエスト数 | 最大ワイヤボディ | 最大システムプロンプト | 最大ツールスキーマ | ツール呼び出し数 | ツール結果合計 | 最大ツール結果 | リクエスト内の最大関数応答 |
| ---------------- | -------------- | -------: | ------------: | ----------------: | --------------: | ---------: | ----------------: | --------------: | -------------------------------: |
| 小さい PR `#4268` | `pai/glm-5`    |        7 |     113.8 KiB |          51.4 KiB |        40.2 KiB |          9 |           4.7 KiB |         3.9 KiB |                         15.3 KiB |
| コードナビゲーション  | `pai/glm-5`    |        2 |     114.6 KiB |          51.5 KiB |        40.2 KiB |          3 |          17.5 KiB |         6.2 KiB |                         18.4 KiB |
| diff 94.6 KiB    | `pai/glm-5`    |        6 |     111.2 KiB |          39.1 KiB |        37.2 KiB |          9 |          94.9 KiB |        92.6 KiB |                         29.2 KiB |
| diff 968.5 KiB   | `pai/glm-5`    |        2 |     104.8 KiB |          39.1 KiB |        37.2 KiB |          2 |         772.1 KiB |       771.9 KiB |                         25.6 KiB |
| diff 4.84 MiB    | `pai/glm-5`    |        4 |     114.7 KiB |          39.1 KiB |        37.2 KiB |          4 |         786.3 KiB |       783.2 KiB |                         34.7 KiB |
| 小さい PR `#4268` | `qwen3.6-plus` |        6 |     134.1 KiB |          51.4 KiB |        40.2 KiB |          5 |          34.6 KiB |        15.6 KiB |                         36.6 KiB |
| コードナビゲーション  | `qwen3.6-plus` |        4 |     114.9 KiB |          51.5 KiB |        40.2 KiB |          3 |          17.5 KiB |         6.2 KiB |                         18.4 KiB |
| diff 94.6 KiB    | `qwen3.6-plus` |        4 |     112.8 KiB |          39.1 KiB |        37.2 KiB |          3 |          92.9 KiB |        92.6 KiB |                         33.0 KiB |
| diff 968.5 KiB   | `qwen3.6-plus` |        6 |     113.1 KiB |          39.1 KiB |        37.2 KiB |          5 |         778.0 KiB |       771.9 KiB |                         32.1 KiB |
| diff 4.84 MiB    | `qwen3.6-plus` |        4 |     121.5 KiB |          39.1 KiB |        37.2 KiB |          4 |         798.5 KiB |       783.2 KiB |                         41.3 KiB |

## 観察結果

1. このローカルバンドル実行では、プロセスツリー RSS はルート RSS とほぼ同じです。ルート/ツリー間のギャップは通常 10 MiB 未満です。つまり、これらの実行では永続的な子プロセスメモリ所有者は見られませんでした。支配的なプロセスはメインの Node プロセスです。
2. ローカルバンドル実行のピークは約 0.36-0.41 GiB であり、以前の 0.83-1.04 GiB ではありません。これは、マトリックスがストリップされた一時設定を使用したためです。フォローアップの通常設定健全性チェックでは、PATH `qwen` とローカル `dist/cli.js` の両方で約 1.1 GiB のツリー RSS が再現され、追加メモリはプロセスツリー内の子 MCP/Node プロセスから発生しました。
3. V8 ヒープは RSS よりもはるかに小さいです。終了時ヒープは約 99-143 MiB ですが、終了時 RSS は約 216-411 MiB です。残りのフットプリントは、読み込まれたモジュール、ネイティブアロケーション、外部バッファ、またはライブ JS ヒープ外のランタイムオーバーヘッドである可能性があります。
4. 静的リクエストオーバーヘッドは大きく、繰り返し発生します。システムプロンプトはリクエストあたり約 39-51 KiB、ツールスキーマはリクエストあたり約 37-40 KiB です。これは、モデルが複数ターン行う場合、小さなタスクでも累積トークン数が高くなる可能性があることを説明しています。
5. 大きな diff 出力は、モデルリクエストに到達する前にキャップされます。968 KiB および 4.84 MiB の diff ケースでは、キャプチャされたツール結果は約 772-799 KiB でしたが、リクエスト内でモデルに表示される最大の関数応答は約 25-41 KiB に留まり、最大ワイヤボディは約 105-122 KiB に留まりました。これは、モデル対向パスでトランケーション/保存出力処理が機能していることを示しています。
6. ワイヤボディが制限されているにもかかわらず、大出力ケースではメモリが依然として増加しています。たとえば、4.84 MiB の GLM 実行では 414.2 MiB のツリー RSS と 410.7 MiB の終了時 RSS に達し、4.84 MiB の qwen3.6-plus 実行では 142.8 MiB のヒープで終了しました。これは、最終リクエストペイロードがキャップされている場合でも、大きなツール出力がローカルキャプチャ、正規化、または保持されたランタイム状態に影響を与える可能性があることを示唆しています。
7. この実行では、モデルの選択により、RSS よりもターン数とトークン合計が変化しました。`qwen3.6-plus` は `pai/glm-5` よりも平均トークン数とターン数が多かったものの、平均ツリー RSS ピークはわずかに低くなりました。これは、モデルの選択がプロセスメモリの主な説明ではないという以前の結論を裏付けています。
## 更新された動作推論

新しい診断により、初期の仮説がより正確になりました。

- インストール済みCLIにおけるユーザーが見る1 GiBのピークは、ローカル診断バンドルで通常構成を使用しても再現可能です。内部のQwenランタイム属性評価にはストリップ版の実行、ユーザーから見えるプロセスツリー属性評価には通常構成の実行を使用する必要があります。
- ストリップ版と通常構成の最大の差異はプロセスツリーの形状です。通常構成では追加のMCP/Node子プロセスが起動します。これらの子プロセスが、最小プロンプトのサニティチェックにおいて約0.35-0.55 GiBから約1.1 GiBへの絶対的なジャンプの大部分を説明します。
- `--bare` フォローアップテストでも `qwen3.6-plus` で同様の傾向が確認されました。同じプロンプト形状で通常構成はベアモードよりもプロセスツリーRSSが約0.50-0.59 GiB多く消費されますが、ルートRSSの変化はわずかです。
- 一時設定分離は `--bare` よりも優れた属性テストです。MCPのみを無効にすると、通常の設定読み込みパスを維持したままプロセスツリーRSSが約0.42-0.47 GiB削減されます。フックのみを無効にした場合、ツール呼び出しなしのケースでは有意なRSS変化は見られません。
- MCPごとの分離により、`chrome-devtools` が主要なMCP貢献者であることが示されました。それだけで高いRSS帯域を再現でき、それを除去すると実行がMCPなしのベースライン近くに戻ります。
- ローカルのQwenランタイム内部では、もはや「モデルに送信される生の差分バイト数」は疑わしい領域ではありません。モデル向けのリクエスト本文は制限されています。
- より強い容疑者は、リクエストごとの静的コンテキストコスト、繰り返しリクエストラウンド、ツールスキーマサイズ、モデル側の切り詰め前または外部での大きなツール出力のローカル保持/キャプチャです。
- RSSがV8ヒープよりもはるかに高いままであるため、次のプロファイリングレイヤーにはモジュール/起動アカウンティング、外部メモリ、ツール実行および最終応答発行前後のヒープスナップショットを含める必要があります。

## 現在の診断によるRSS属性

現在のカウンターは正確な保持オブジェクトやソースファイルを特定していませんが、これらのローカル実行でRSSを駆動するものと駆動しないものを絞り込んでいます。

| シグナル                         | 現在のエビデンス                                                                                                                          | RSSへの影響                                                                                                                                |
| ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| ルートRSS vs プロセスツリーRSS       | ルートとツリーのピークは通常約2-10 MiBの範囲内；DeepSeek large PRで最大の差約23.6 MiB                                                          | このローカルバンドル実行では、持続的な子プロセスがRSSを説明していない；メインのNodeプロセスが支配的                                          |
| 通常構成のプロセスツリー              | 最小プロンプト通常構成の実行でツリーRSS約1.1 GiBに達する一方、ルートRSSは約213-250 MiBに留まる                                                | ユーザーが見る1 GiBのピークは、QwenルートRSS単独ではなく、MCP/Node子プロセスによって支配される可能性がある                                    |
| `--bare` 比較                   | `qwen3.6-plus` 通常実行のピークはおよそ1.02-1.05 GiBのツリーRSS；ベア実行のピークはおよそ0.45-0.53 GiB                                        | この環境で通常構成をロードすると、プロセスツリーRSSが約0.50-0.59 GiB追加される                                                               |
| 一時的MCP分離                    | MCPサーバーをクリアすると、起動/構成のツリーRSSが865-1,017 MiBから443-549 MiBに低下                                                             | 制御構成チェックにおいて、MCP起動とMCP子プロセスがプロセスツリーRSSの約0.42-0.47 GiBを説明                                                   |
| MCPごとの分離                    | `chrome-devtools` 単独で繰り返しサンプルで約1.0 GiBに達する；それを除くと実行は約461 MiBに留まる                                                    | この環境で `chrome-devtools` が主要なMCPプロセスツリーRSS貢献者                                                                                |
| 一時的フック分離                  | `disableAllHooks=true` でMCPが有効のままの場合、ツール呼び出しなしのケースでツリーRSSの変化は約13-18 MiBのみ                                               | ここではフック構成だけでは可視の起動RSSドライバーではない；フック実行にはツール呼び出しベンチマークが必要                                          |
| V8ヒープ vs RSS                | 終了時ヒープは約99-143 MiBに対し終了時RSSは約216-411 MiB                                                                                    | ライブJSヒープだけがフットプリント全体ではない；ロードされたモジュール、ネイティブ割り当て、外部バッファ、ランタイムオーバーヘッドが重要と考えられる     |
| PR/差分サイズ vs RSS            | DeepSeek小/中/大PRは変更行数1〜4,750までスケールするが、ツリーRSSは340.7-360.0 MiBの狭い帯域に留まる                                            | ツール出力が制限されている場合、生のPRサイズはRSSを線形に駆動しない                                                                              |
| ツール出力サイズ                   | 大きな差分実行では約772-799 KiBのツール結果をキャプチャし、一部でより高い終了時RSS/ヒープを示すが、RSSは線形にスケールしない                          | ツール結果のキャプチャ/正規化が圧力を加える（特に大出力ケース）が、唯一のRSSドライバーである可能性は低い                                          |
| リクエスト本文サイズ               | モデル向け本文の最大値は約103-289 KiBである一方、RSSは同じ帯域に留まる                                                                          | リクエストシリアライズサイズはトークンとレイテンシに明確に影響を与えるが、RSSピークにはあまり影響しない                                        |
| リクエストごとの静的コンテキスト       | システムプロンプトは約39-51 KiB、ツールスキーマはリクエストあたり約37-48 KiB                                                                    | 繰り返しラウンドはトークン/コストの増幅器；これ単独ではRSSを説明しないが、トークン圧力の最適化ターゲットとして可能性が高い                          |

現在の属性：ストリップ版ローカルバンドルベンチマークでは、RSSフロアは主にタスク時間のランタイム/モジュール/ネイティブフットプリントであり、大きなツール出力が増分的な圧力を追加します。通常構成の実行では、ユーザーが見る1 GiBのツリーピークは主にプロセスツリーの構成です：QwenルートとMCP/Node子プロセスです。次のターゲット計測では、Qwenルート診断と設定されたMCPサーバー診断を分割し、Qwenルートプロセス内に起動/モジュール/外部メモリのチェックポイントを追加する必要があります。

## 進捗スナップショット

現在確認されたシグナル：

1. ユーザーが見る1 GiBの起動/構成ピークは、インストール済みCLIとローカル診断バンドルの両方で通常構成をロードしたときに再現可能です。診断ブランチやPR `#4186` が主な原因ではありません。
2. この環境では、その1 GiBピークは主にプロセスツリーの構成です：Qwenルートプロセス＋再起動子プロセス＋MCP子プロセス。
3. `chrome-devtools` が現在の構成における主要な設定済みMCP貢献者です。プロンプトがそのMCPを明示的に使用しない場合でも、それだけで高いプロセスツリーRSS帯域を再現できます。
4. MCPなしの通常再起動形状は、プロセスツリーRSS約0.45 GiBに留まります。再起動親プロセスなしの単一Qwenランタイムプロセスは、起動属性チェックで約0.22-0.24 GiBに近い値です。これは、0.45 GiBのベースラインが単一プロセスのルートRSS数値ではないことを意味します。
5. ストリップ版の非対話型タスク実行では、モデルの選択がターン数、トークン合計、レイテンシ、リクエストサイズにRSSよりも明確に影響を与えます。RSSは `pai/glm-5`、`qwen3.6-plus`、`DeepSeek/deepseek-v4-pro` 間で比較的狭い範囲に留まりました。
6. 現在の短タスク診断では、モデル向けのツール/関数応答は制限されていますが、ローカルのツール結果キャプチャとランタイム状態は、大出力ケースでヒープ/RSSを増加させる可能性があります。これにより、大出力保持が調査パスに残ります。

現在のギャップ：

1. 短タスクベンチマークマトリックスはまだ短命です。後の対話型長時間レビュー実行で41.9分の障害を再現しましたが、まだ1サンプルであり、繰り返し実行とヒープ/オブジェクト属性が必要です。
2. 現在のカウンターはプロセスツリーRSSとリクエストサイズの属性には十分ですが、長時間セッション中の保持されたJSオブジェクトグラフを命名するには不十分です。
3. 起動/構成RSSと長時間セッションOOMは別々のトラックとして維持する必要があります。MCPと再起動はアイドル/起動RSS帯域の大部分を説明しますが、それだけでは長時間タスク後のV8ヒープOOMを説明しません。
4. 対話型TUIメモリは非対話型モードとは別の実行が必要です。UI履歴とInk静的出力が同じように実行されないためです。

## イシューとPRからの長時間タスクOOMエビデンス

イシュー/PRのエビデンスは、単一の障害モードではなく、いくつかの異なるOOM形状を指しています。

| ソース                                                                                                               | エビデンスの要約                                                                                                                                       | テストすべき仮説                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| [`#4309`](https://github.com/QwenLM/qwen-code/issues/4309)                                                           | ユーザーがYOLOモードとDeepSeekバックエンドで5.84 GiBのメモリ使用量 / 7.02 GiBの警告を報告；Nodeメモリを8 GiBに増やしても症状は除去されなかった                       | 長い自律ツールループは、old-space制限を単に増やすだけでは根本的な修正にならないほど多くの状態を保持できる                       |
| [`#4149`](https://github.com/QwenLM/qwen-code/issues/4149)                                                           | 複数の報告で `Ineffective mark-compacts near heap limit` が示されており、4 GiBやそれよりはるかに大きなヒープ制限のケースを含む                               | ヒープの大部分は到達可能なアプリケーション状態であり、すぐに収集可能なガベージではない                                           |
| [`#4116`](https://github.com/QwenLM/qwen-code/issues/4116)                                                           | OOMはコンテキスト表示が約9.5%のときに発生；分析は `structuredClone`、UI履歴、Ink静的ツリー、大きなコンテキストウィンドウを指摘                             | トークン使用量が低くてもJSヒープ圧力が高い可能性がある；トークン閾値だけでは信頼性のあるメモリガードではない                      |
| [`#4167`](https://github.com/QwenLM/qwen-code/issues/4167)                                                           | ユーザーは圧縮中にクラッシュしたと報告；分析は圧縮ピークメモリが別の形状であることを特定                                                                    | 圧縮自体がヒープが既に高いときにピークを引き起こす可能性がある（特に履歴が同時期にクローン/文字列化される場合）                  |
| [`#2128`](https://github.com/QwenLM/qwen-code/issues/2128)                                                           | 報告は無制限のUI履歴、保持されたファイル差分/端末出力、string-widthキャッシュ、チェックポイントシリアライゼーションを特定                                | 対話型TUIの長時間セッションは、モデル履歴外および非対話型ベンチマーク外でメモリを保持する可能性がある                           |
| [`#2562`](https://github.com/QwenLM/qwen-code/issues/2562)                                                           | 報告は `GeminiChat.getHistory()` が長時間セッションで全履歴をディープクローンすることに焦点を当てる                                                    | 全履歴クローンはメモリピークを増幅する可能性があり、保持された定常状態サイズとは別に測定すべき                           |
| [`#4185`](https://github.com/QwenLM/qwen-code/issues/4185)                                                           | トークンベースのコンパクションが実行される前にV8ヒープ圧力が制限を超える状況を追跡                                                                          | ヒープ圧力ガードは必要だが、保持データが大きいままの場合、症状を軽減するだけに留まる                    |
| [`#4184`](https://github.com/QwenLM/qwen-code/issues/4184)                                                           | 保持された大きなツール結果に対する診断とオフロード/プレビューを提案                                                                                    | 大きなツール出力はモデルリクエストに対して制限されていても、ローカルのホットメモリに保持されている可能性がある                          |
| [`#4186`](https://github.com/QwenLM/qwen-code/pull/4186)                                                               | マージされたヒープ圧力自動コンパクションのセーフティネットと `nextSpeakerChecker` のO(1)最終履歴アクセス                                                     | ヒープ圧力とクローン増幅の一部をカバーするが、すべてのOOMクラスを解決するわけではない                                                |
| [`#4127`](https://github.com/QwenLM/qwen-code/pull/4127), [`#4168`](https://github.com/QwenLM/qwen-code/pull/4168)     | コンパクション閾値に関するオープンPR；一方は固定ヒープ閾値、他方はトークン閾値と圧縮動作を再設計                                                          | 有用な関連作業だが、長時間タスクテストではヒープ、トークン、圧縮シグナルが実際の実行で一致するかを検証する必要がある                 |
| [`#3000`](https://github.com/QwenLM/qwen-code/issues/3000), [`#4183`](https://github.com/QwenLM/qwen-code/issues/4183) | 診断ロードマップは `/doctor memory`、ヒープスナップショット、制限付きメモリタイムラインを挙げている                                                       | スナップショット/タイムラインサポートは、RSS属性から保持オブジェクト属性に移行するために必要                                   |

初期解釈：

- 未使用の設定済みMCPはメモリを消費する可能性があります。通常の起動では設定済みMCPサーバーに接続し、タスクが必要とする前にそれらのツールをアドバタイズするためです。測定された構成では、`chrome-devtools` が追加のNode/npm MCPプロセスを起動し、ツールスキーマ数が19から48に増加します。これは大きな起動/構成RSS帯域を説明し、繰り返しリクエストのオーバーヘッドも増加させる可能性があります。
- 長時間セッションOOMレポートは別のレイヤーです。Mark-Compactがほとんどメモリを解放しないGCログは、ヒープが到達可能な状態で満たされていることを示唆します。最も有力な候補は、保持された履歴/ツール/UIオブジェクト、全履歴クローン、圧縮中間データ、ストリーミング/ログ集約器です。
- PR `#4186` は有用な緩和策です。トークン閾値がトリガーされる前にヒープ圧力に基づいてコンパクションを実行でき、不要な全履歴クローンを1つ除去します。しかし、大きなツール出力保持、UI履歴保持、または圧縮ピークメモリがすでに解決されたという証明として扱うべきではありません。

## 長時間タスク検証計画

次のベンチマークは2つのトラックを分離して維持する必要があります：

1. 起動/構成属性評価：通常構成 vs MCP無効 vs `chrome-devtools`のみ vs 再起動なしの属性評価。これにより、有意義な作業が始まる前にユーザーが見るものを説明します。
2. 長時間タスクの実行時増加：繰り返しツール呼び出し、大きな出力、圧縮、再開、対話型UI履歴。これにより、実際の作業後のOOMを説明します。

推奨される長時間タスクケース：

| ケース                          | 形状                                                                                                | 重要である理由                                                                                    |
| ----------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 長いPRレビューループ           | 中/大のPRレビュープロンプトを30分、60分、120分繰り返し、固定モデルと固定構成                                | 報告されているエージェントワークフローに最も近い；ターン数、ツール呼び出し、トークン増加、RSS/ヒープ傾向を捉える |
| 大きなツール出力保持           | 制限された1 MiB / 5 MiB / 20 MiBのコマンド出力を繰り返し生成し、フォローアップ質問を行う                     | モデル側の切り詰め後に生の出力がローカルに保持されるかどうかをテスト                     |
| 圧縮圧力                      | より低い制御されたold-space制限と大きなコンテキストプロンプトを使用して、ヒープ圧力コンパクションをトリガー         | PR `#4186` がOOM前にトリガーされるかどうか、および圧縮自体が新しいピークを生成するかどうかを検証         |
| 対話型TUI履歴                 | 同じ長いループをtmux TUIモードで実行し、非対話型モードと比較する                                       | UI履歴、Ink静的出力、レンダリングされた差分、端末出力表示の保持を分離               |
| 再開ストレス                   | 保存された大きなセッションを再開し、すぐに作業を続行                                                   | `/resume` OOMレポートとセッション再構築コストをターゲット                            |
| ストリーミング/ログ集約器       | テレメトリ/ログを有効/無効にして長いストリーム応答を強制                                       | イシュー分析から疑われる `collected responses` / ログ保持パスをテスト          |
| MCPアイドル vs MCPアクティブ   | MCPなし、`chrome-devtools` 設定済みだが未使用、`chrome-devtools` アクティブ使用のバリエーションを実行       | アイドルMCP子RSSと実際のMCPツール実行およびツールスキーマ/トークンオーバーヘッドを分離 |

ターンごとまたはサンプリング間隔ごとに記録すべきメトリクス：

- ルートRSS現在/ピーク、プロセスツリーRSS現在/ピーク。
- 子プロセス数、上位子プロセスのコマンド形状。
- V8 `heapUsed`、`heapTotal`、`heap_size_limit`、`external`、`arrayBuffers`。
- ターン数、リクエスト数、ツール呼び出し数、ツール呼び出しラウンド数。
- リクエストごとおよびタスク全体の入力/出力/キャッシュ/合計トークン数。
- リクエスト本文バイト数、システムプロンプトバイト数、ツールスキーマバイト数、関数応答バイト数。
- ツール結果数、キャプチャされたツール結果の合計バイト数、最大ツール結果バイト数、保持されたツール結果バイト数（利用可能な場合）。
- 会話履歴メッセージ数とおおよその履歴バイトサイズ。
- 対話型のみのUI履歴項目数と保持された表示サイズのおおよその値。
- 圧縮試行回数、圧縮トリガー理由、圧縮前後のトークン数、圧縮前後のヒープ圧力、圧縮失敗ステータス。
- ヒープスナップショットまたは制限付きメモリタイムラインのアーティファクト（ヒープ圧力が設定された閾値を超えた場合）。

検証基準：

1. 少なくとも主要な長時間タスクケースを2回繰り返すこと。起動RSSには可視のバラつきがあるため、単回実行の結論は避けるべき。
2. ルートRSSとプロセスツリーRSSを別々に報告すること。ユーザーから見えるメモリ圧力は子プロセスから発生する可能性があり、V8 OOMはQwenルートヒープから発生する。
3. 平坦なRSS線を重要なエビデンスとして扱うこと。トークンとツール呼び出しが増加してもヒープ/RSSが平坦な場合、問題はおそらく別の場所にある。
4. RSSまたはヒープが成長する場合、その成長を特定のシグナルと相関させること：ツール結果バイト数、履歴バイト数、UI履歴数、圧縮イベント、ストリーミング集約器サイズ、MCPプロセス起動。
5. ヒープスナップショットを取得する場合、まず構造化診断JSONを書き、その後スナップショットを取得すること。ヒープスナップショットは大きく、機密文字列を含む可能性があるため、オプトインかつローカルに留めるべき。

## 対話型長時間レビューの再現

短い非対話型プロンプトが目標ウィンドウ前に終了し続けた後、リモート入力を使った対話型TUIベンチマークが実行されました。CLIプロセスは1つのセッションで存続し、コントローラが一度に1つの実際のPRレビューターンを送信しました。次のターンは、アシスタントがそのターンの完了マーカーを発行した後にのみ送信されました。これにより、短いワンショットプロンプトを長時間タスクの再現として扱うことを避けています。
セットアップ:

- Qwen Code `0.15.11` をインストール、モデルは `qwen-latest-series-invite-beta-v28`。
- 通常の設定から一時的なCLIホームを生成し、MCPとフックの設定を削除。グローバル設定は変更なし。
- インタラクティブTUIモード、デュアルJSONイベント出力、リモートJSONL入力。
- 静的PRレビューのみ。プロンプトで依存関係のインストール、ビルド、テスト、Playwright、Docker、その他の長時間の外部ビルドコマンドを禁止。
- 外部RSSサンプラーは5秒ごとにプロセスツリーRSSとQwen root RSSの両方を記録。

結果:

| シグナル                        |        値 |
| ------------------------------- | --------: |
| 終了までのウォールタイム        |    41.9 min |
| 終了ステータス                  |           1 |
| 完了したPRレビューターン        |           6 |
| メインチャットレコード          |       1,076 |
| API応答テレメトリー             |         335 |
| ツール呼び出しテレメトリー      |         607 |
| MCPツール呼び出しテレメトリー   |           0 |
| メイン/root API応答             |          36 |
| サブエージェントAPI応答         |         299 |
| root合計トークン                |       2.08M |
| サブエージェント合計トークン    |      17.24M |
| APIテレメトリートークン合計     |      19.32M |
| root最大入力トークン            |      85,655 |
| サブエージェント最大入力トークン|     215,207 |
| `/usr/bin/time -l` 最大RSS      | 1,072.4 MiB |
| サンプリングQwen root RSSピーク | 1,028.2 MiB |
| サンプリングプロセスツリーRSSピーク | 1,038.1 MiB |

プロセスは次のエラーで終了:

```text
libc++abi: terminating due to uncaught exception of type std::__1::system_error: thread constructor failed: Resource temporarily unavailable
```

これは**スレッド枯渇**エラーであり、V8ヒープOOMではない。障害メカニズムは明確に異なる。OSが新しいスレッドの作成を拒否した。原因はおそらくプロセスごとのリソース制限（`RLIMIT_NPROC`）またはメモリ断片化によるスタック割り当ての失敗である。MCPを無効にし、ビルド/テストなし、インタラクティブな長時間セッションレビューで、Qwen Nodeプロセス自体が約1 GiBのRSSを超えた場合に発生したため、関連性がある。
障害は最終サマリーフェーズ中に発生し、コントローラーはすでに6回のレビューターンを完了していた。

ターンタイムラインとサンプリングされたQwen root RSS:

| 時間帯         | ターン状態           | Qwen root RSS最大 | 時間帯終了時のQwen root RSS |
| -------------- | -------------------- | ----------------: | --------------------------: |
| 0.0-9.0分      | ターン1完了          |         701.2 MiB |                   255.3 MiB |
| 9.0-15.1分     | ターン2完了          |         503.2 MiB |                   494.4 MiB |
| 15.1-24.1分    | ターン3完了          |         468.7 MiB |                   457.5 MiB |
| 24.1-31.9分    | ターン4完了          |         619.3 MiB |                   602.3 MiB |
| 31.9-40.3分    | ターン5完了          |         955.5 MiB |                   955.5 MiB |
| 40.3-40.4分    | ターン6完了          |         988.6 MiB |                   988.6 MiB |
| 40.4-41.9分    | 最終サマリー/終了    |       1,028.2 MiB |                 1,028.2 MiB |

トークンとツールの分布:

| 所有者        | API応答数 | 入力トークン | 出力トークン | 合計トークン | 最大入力 |
| ------------- | --------: | -----------: | ------------: | -----------: | --------: |
| Rootセッション|         36 |        2.06M |         22.2K |        2.08M |    85,655 |
| サブエージェント|       299 |       17.08M |        154.6K |       17.24M |   215,207 |

関数別ツール呼び出しテレメトリー:

| ツール               | 呼び出し回数 | キャプチャコンテンツ長 |
| -------------------- | ----------: | ----------------------: |
| `read_file`          |         271 |                 1.46 MB |
| `run_shell_command`  |         181 |                164.4 KB |
| `web_fetch`          |          80 |                846.3 KB |
| `grep_search`        |          25 |                 15.0 KB |
| `glob`               |          15 |                 27.8 KB |
| `todo_write`         |          16 |                 16.1 KB |
| `list_directory`     |           8 |                  6.2 KB |
| `agent`              |          10 |                       0 |
| `tool_search`        |           1 |                  2.1 KB |

単一エージェントのTUIトークンカウンターの最大表示は約3.83Mトークンに達した。テレメトリーによると、最も負荷の高いサブエージェントは合計約4.05Mトークンで、最大入力要求は215Kトークンだった。この再現では、サブエージェントの増幅が支配的なシグナルである。

解釈:

1. この実行は、長時間セッションのメモリ増加とMCP起動/設定メモリを分離している。MCPは無効化されており、MCPツール呼び出しはなかったが、Qwen rootプロセスは依然として約1 GiBのRSSに達した。
2. 後半のメモリピークは、サブエージェントを多用したレビューターンと最終サマリー/マージバックに一致しており、外部のビルド/テスト子プロセスには一致しない。
3. RSS曲線は単純な線形リークではない。初期のターン後に減少し、後半のサブエージェントターン後に急激に上昇し、終了間際に高いまま維持される。
4. 障害モードはV8ヒープ制限スタックではなくネイティブリソース枯渇であるため、次の実行ではヒープ/external/arrayBuffer/スレッド数のサンプリングを追加すべきである。RSSだけではJSヒープとネイティブ割り当てやスレッドリソース圧力を区別できない。
5. 調査すべき最も強いコードパスは、サブエージェントのトランスクリプト保持、エージェント結果のマージバック、全履歴のクローン、チェックポイント/セッション記録、そして最終サマリー/履歴アセンブリである。

## 決定論的大規模タスククローン圧力再現

`scripts/memory-pressure-repro.mjs` として決定論的なストレスハーネスを追加。このハーネスはモデルを呼び出さない。代わりに、rootレビューターン、サブエージェントトランスクリプト、大きなツール結果、チェックポイントJSON、保持された `structuredClone()` コピーを含む、Qwenのような長時間セッションオブジェクトグラフを構築する。これにより、ユーザー提供のOOMスタックから疑われるクローンとチェックポイントのピークを再現可能にする。

ハーネスには軽量スクリプトテストがある:

```bash
npx vitest run --config ./scripts/tests/vitest.config.ts \
  scripts/tests/memory-pressure-repro.test.js
```

結果: 合格、テスト1件。

制御された実行では、特に明記しない限り `node --max-old-space-size=256` を使用。

| ケース                                           | 履歴の形状                                                           | クローン/チェックポイントの圧力                          | 結果                            |   最大RSS |
| ------------------------------------------------ | -------------------------------------------------------------------- | -------------------------------------------------------- | ------------------------------ | --------: |
| 小規模サニティ                                    | 2ターン、2 KiBツール結果、1サブエージェント                           | 1クローン + 1チェックポイント                             | 合格; 2.6 MiB 履歴JSON         |  89.7 MiB |
| 巨大ビルドのみ                                    | 12ターン、256 KiBツール結果、2サブエージェント × 12サブエージェントターン | クローン/チェックポイントを保持せず                   | 合格; 76.2 MiB 履歴JSON        | 491.5 MiB |
| 巨大 + 1クローン                                  | 同上                                                                 | 保持された `structuredClone()` 1つ                       | 合格                           | 569.6 MiB |
| 巨大 + 2クローン                                  | 同上                                                                 | 保持された `structuredClone()` コピー2つ                 | OOM, exit 134                  | 496.5 MiB |
| 巨大 + 1チェックポイント                          | 同上                                                                 | 元の履歴JSON + クローン履歴JSONを含む1つのチェックポイント | 合格; 152.5 MiB チェックポイントJSON | 926.9 MiB |
| 巨大 + 2チェックポイント                          | 同上                                                                 | 2つのチェックポイントコピー                              | OOM, exit 134                  | 920.1 MiB |
| 巨大 + 2クローン、サブエージェントトランスクリプト保持なし | 同じ生成サブエージェント出力だが、親履歴はサマリーのみ保持         | 合格; 親履歴JSONが3.8 MiBに低下       | 136.8 MiB                         |

失敗した巨大クローン実行の出力:

```text
FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory
```

ネイティブスタックには以下が含まれる:

- `v8::internal::ValueDeserializer::ReadObjectInternal`
- `v8::internal::ValueDeserializer::ReadDenseJSArray`
- `node::worker::Message::Deserialize`
- `node::worker::StructuredClone`

これはユーザー提供のOOMログと同じスタックファミリーと一致する。制御された再現は、4 GiB / 8 GiBのユーザーレポートが妥当である理由も示している。障害は単一の大きなオブジェクトではなく、大きな保持履歴/ツール結果/サブエージェント状態に加えて、1つ以上の全履歴クローンまたはチェックポイントコピーによって引き起こされる。`--max-old-space-size` を増やすと、同じ増幅パターンを維持しながらクラッシュを遅らせることができる。

この決定論的実行からの重要な帰属:

1. 76.2 MiBの親履歴JSONを削減されたヒープで構築することは成功する。OOMは追加の全履歴クローン/チェックポイントコピーが保持されたときに現れる。
2. 1つのチェックポイントコピーだけで、OOM前でもRSSを1 GiB近くまで押し上げることができる。
3. 親ホット履歴から保持されたサブエージェントトランスクリプトを削除すると、同じ生成ワークロードがOOMから136.8 MiBの小さなRSS実行に変わる。これが今のところ最も明確な緩和シグナルである。
4. この再現器は合成であり、意図的に敵対的だが、長いインタラクティブレビューと同じオブジェクトグラフ形状（親セッション、サブエージェント、大きなツール出力、トランスクリプトマージバック、全履歴クローン圧力）を実行している。

## DeepSeek PRサイズフォローアップ

初期モデルマトリックスの後、追加のQwen Codeのみの実行で、3つの実際のPRサイズに対して `DeepSeek/deepseek-v4-pro` をテストした。このモデルはAnthropic互換プロトコルで設定される。OpenAI互換実行はスモークチェックで404を返したため、正常なベンチマークは `--auth-type anthropic` を使用する。

診断ブランチを拡張し、OpenAIパスと同じプライバシールールでAnthropicワイヤー要求サマリーを記録するようにした。集計数とバイトサイズのみ、プロンプトテキスト、差分コンテンツ、ツール引数、ヘッダー、ベースURL、APIキーは含まない。

PRサイズ:

| サイズ  | PR      | 状態   | ファイル数 | 変更行数 | タイトル                                                                |
| ------- | ------- | ------ | --------: | --------: | ----------------------------------------------------------------------- |
| small   | `#4268` | マージ済 |         1 |         1 | fix(serve): add mcp_guardrails to E2E capabilities expectation          |
| medium  | `#4186` | マージ済 |         6 |       494 | fix(core): add heap-pressure auto-compaction safety net                 |
| large   | `#4168` | オープン |        25 |     4,750 | feat(core)!: redesign auto-compaction thresholds with three-tier ladder |

実行時間:

| サイズ  | PR      |   ウォール | ターン数 | 合計トークン | キャッシュ読み取りトークン | ツリーRSSピーク | Root RSSピーク | 終了時ヒープ | 終了時RSS |
| ------- | ------- | --------: | ------: | -----------: | -----------------------: | --------------: | --------------: | ----------: | --------: |
| small   | `#4268` |     39.7s |       2 |       43,362 |                    28,672 |       346.9 MiB |       344.8 MiB |   115.2 MiB | 304.3 MiB |
| medium  | `#4186` |    142.6s |       4 |      135,120 |                   115,840 |       340.7 MiB |       337.3 MiB |   103.5 MiB | 285.6 MiB |
| large   | `#4168` |    191.1s |       8 |      386,891 |                   332,928 |       360.0 MiB |       336.3 MiB |   119.3 MiB | 237.9 MiB |

リクエストとツール診断:

| サイズ  | PR      | リクエスト数 | Anthropicワイヤーリクエスト数 | Anthropicボディ最大 | 最大システム | 最大ツールスキーマ | ツール呼び出し | ツール結果合計 | 最大ツール結果 | リクエスト内最大関数応答 |
| ------- | ------- | ----------: | --------------------------: | -----------------: | ---------: | ----------------: | ------------: | --------------: | --------------: | -----------------------: |
| small   | `#4268` |           2 |                           2 |          103.0 KiB |   50.8 KiB |          47.6 KiB |            3 |         0.6 KiB |         0.5 KiB |                  1.1 KiB |
| medium  | `#4186` |           4 |                           4 |          159.8 KiB |   50.8 KiB |          47.6 KiB |            5 |        30.2 KiB |        29.3 KiB |                 56.7 KiB |
| large   | `#4168` |           8 |                           8 |          289.5 KiB |   50.8 KiB |          47.6 KiB |           11 |       235.0 KiB |       232.1 KiB |                182.4 KiB |

DeepSeekの観察:

1. PRサイズに応じて、ターン数、トークン数、Anthropicワイヤーボディサイズ、ツール結果サイズは明確にスケールしたが、RSSは比例してスケールしなかった。小/中/大のツリーRSSピークは狭い `340.7-360.0 MiB` の帯域に収まった。
2. 大きなPRは主にモデルラウンド数とトークン量の点でコストが高かった。8リクエスト、合計386,891トークン。最大Anthropicボディは289.5 KiBで、OpenAI互換実行よりもはるかに大きかったが、RSSは依然として同じローカルバンドル帯域付近に留まった。
3. 静的Anthropicリクエストコストも顕著である。システムプロンプトは約50.8 KiB、ツールスキーマはリクエストあたり約47.6 KiB。そのため、ラウンドの繰り返しは主要なトークン増幅器となる。
4. 大きなPRでは、キャプチャされたツール結果が235.0 KiB、リクエスト内の最大関数応答が182.4 KiBになった。これは以前の小さなPR/コードナビゲーションケースよりも高く、RSSが急上昇しなくても、大きなPRはローカルツール結果処理とリクエストアセンブリに圧力をかけることを示している。
5. DeepSeek実行はモデル選択の結論を強化する。プロバイダー/モデルの選択はターン数、レイテンシ、トークン量、ワイヤーペイロード形状を強く変えるが、ローカルバンドルRSSピークはQwen Codeランタイム形状に支配され、PRサイズに線形に比例しない。

## 長時間レビューJSONLリプレイ: 履歴クローン圧力

最近の長いPRレビューチャット記録を、報告されたOOMクラスの事後分析形状として分析した。生のJSONLはプロンプトとツール出力テキストを含むため、ここには含めない。集計形状は以下の通り:

| シグナル                  | 値                            |
| ------------------------- | ----------------------------- |
| 期間                      | 87.0 min                      |
| Qwen Codeバージョン       | 0.15.10                       |
| モデル                    | qwen-latest-series beta model |
| API応答                   | 380                           |
| ツール呼び出しテレメトリー| 507 イベント                  |
| MCPツール呼び出しテレメトリー| 4 イベント                    |
| サブエージェントAPI応答   | 313                           |
| Root API応答              | 67                            |
| Rootプロンプト増加        | 38,622 -> 168,555 トークン    |
| 最大プロンプトトークン    | 168,555                       |
| 合計応答トークン          | 31.28M                        |

この形状は、このケースの主要なOOM原因としてMCPを支持しない。507のツール呼び出しテレメトリーイベントのうち、MCPは4つだけで、4つすべてが `content_length=0` を記録していた。支配的な形状は長時間セッション/サブエージェント増幅である。15回の `agent` 呼び出しが313のサブエージェントAPI応答と403のサブエージェントツール呼び出しイベントを生成した。

リプレイでは、JSONLからチャットの `Content[]` メッセージ形状を再構築し、制御されたクローン/stringify圧力テストを実行した。基本保持メッセージペイロードは小さいため、それ自体ではOOMしない:

| リプレイ倍率 | 保持クローン数 | 履歴JSON | チェックポイントJSON | 終了時ヒープ | 終了時RSS |
| ------------ | --------------: | --------: | ----------------: | ----------: | --------: |
| 1x           |               8 |   0.54 MB |          1.08 MB |   18.0 MB  |  88.8 MB  |
| 30x          |               8 |  14.46 MB |         28.92 MB |  260.0 MB  | 577.8 MB  |
| 60x          |               8 |  28.86 MB |         57.71 MB |  510.3 MB  | 960.8 MB  |

スケーリングされたリプレイはユーザーデータの主張ではない。観測されたJSONL形状の制御された増幅であり、全履歴クローンとチェックポイントシリアライゼーションがレポートと同じ障害モードを引き起こすことができるかどうかをテストするためのものである。

`--max-old-space-size=256` での低ヒープ再現でメカニズムが確認された:

| ケース                   | 履歴JSON | 結果                                                |
| ------------------------ | --------: | -------------------------------------------------- |
| 履歴のみビルド           |   38.4 MB | 成功; ヒープ131.6 MB, RSS 378.2 MB                |
| ビルド + 1クローン       |   38.4 MB | 成功; ヒープ183.3 MB, RSS 463.4 MB                |
| ビルド + 複数クローン    |   38.4 MB | 複数の保持された `structuredClone()` コピー後にOOM |
| チェックポイント二重履歴|   38.4 MB | 履歴とクローンクライアント履歴を保持中にOOM        |

複数クローンOOMスタックには `ValueDeserializer::ReadObjectInternal`、`ValueDeserializer::ReadDenseJSArray`、`node::worker::Message::Deserialize`、`node::worker::StructuredClone` が含まれ、ユーザー提供のOOMログと同じスタックファミリーと一致する。これは、全履歴の `structuredClone()` がMCPサーバーの関与なしに即時のOOMトリガーになり得ることを証明している。

このJSONLクラスに関する現在の作業仮説:

1. MCPは通常設定の起動RSSを別のベンチマークで説明できるが、この長時間レビューOOM形状の引き金としては可能性が低い。
2. 長時間タスクのメモリ増加は、保持されたチャット履歴、大きなツール出力、サブエージェント履歴、可観測エージェントメッセージ、UI/ツール結果状態に起因する。
3. 即時のOOMトリガーは、ヒープがすでに高い状態での全履歴クローンまたはチェックポイントスタイルの二重シリアライゼーションである可能性がある。
4. 圧縮は保持履歴を軽減できるが、圧縮自体が最初に大きな履歴をクローンまたはシリアライズする場合、一時的なピークを生み出す可能性がある。

### ローカル緩和検証: MCP無効PRレビューケース

2つの対象を絞った緩和策をローカルで適用し、MCP無効PRレビューケースを再実行する前に検証した:

1. `checkNextSpeaker()` は現在、最後のキュレーションメッセージのみを `getHistoryTail(1, true)` で読み取り、そのメッセージのみを次のスピーカーサイドクエリに送信する。次のスピーカープロンプトは直前のモデル応答についてのみ尋ねるため、全履歴を送信するのは不要なクローンとトークン圧力だった。
2. `AgentToolInvocation` は、ライブの `task_execution.toolCalls` 表示内に完全な `responseParts` 配列を保持しなくなった。実際の応答パーツはトランスクリプト/履歴パスを依然として流れるが、長時間実行中に大きなサブエージェントツール出力の別の完全コピーを保持する代わりに、親UI表示はネストされたツール結果ストリーミングのために制限されたテキストサマリーのみを保持する。
3. `GeminiChat.sendMessageStream()` は、パブリックな `getHistory(true)` を呼び出す代わりに、内部のキュレーション履歴ビューを介してモデルリクエストコンテンツを構築するようになった。パブリックな `getHistory()` は外部呼び出し元に対して防御的な `structuredClone()` を依然として返すが、リクエストホットパスはすべてのモデル呼び出しの前に保持されたチャット履歴全体をディープクローンしなくなった。

これらの緩和策に対してTDDチェックを追加:

| テスト                                                                                                           | 期待される保護                                                                 |
| ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `checkNextSpeaker > 最後のキュレーションモデルメッセージのみをサイドクエリに送信すること`                         | 次のスピーカーチェックでの全履歴クローン/送信を防止                           |
| `AgentTool > TOOL_RESULT後にライブツール呼び出し表示にresponsePartsを保持しないこと`                              | ライブサブエージェント表示が大きなツール応答を保持するのを防止                 |
| `AgentTool > ライブツール呼び出し表示に制限された結果サマリーのみを保持すること`                                  | 完全な応答本文を保持せずにネストされた結果の可読性を維持                       |
| `GeminiChat > sendMessageStream > リクエストコンテンツ構築時に全キュレーション履歴をディープクローンしないこと`   | リクエストセットアップが `ValueDeserializer` / `StructuredClone` OOMパスに当たるのを防止 |
追加の再現および修正検証:

| 手順                                        | コマンド形状                                                                                                                               | 結果                                                                                                                                |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 修正前の決定論的クローン負荷テスト              | `node --max-old-space-size=256 scripts/memory-pressure-repro.mjs ... --clone-count=2 --mode=clone`                                           | OOM、exit 134; stderrに `Reached heap limit` と `ValueDeserializer` / `StructuredClone` が含まれていた; 繰り返し実行時の最大RSSは528.1 MiB |
| レッドテスト                              | リクエストセットアップ中に `structuredClone` を強制的にスローさせた `GeminiChat` テストを対象                                         | 修正前に `GeminiChat.getHistory()` で失敗                                                                                            |
| グリーンテスト                            | 修正後の同じ `GeminiChat` テストを対象                                                                                                   | 成功                                                                                                                                |
| ビルドコードのスモークテスト              | `node --max-old-space-size=256` でビルド済み core パッケージに対して、96エントリ/約48 MiBの履歴と `structuredClone` を強制スロー実行         | 成功; リクエストに97個のコンテンツあり; プロセスRSS 161.4 MiB、`/usr/bin/time -l` 最大RSS 161.6 MiB                                          |

これにより、以前の「同一スタックファミリー」との記述が絞り込まれます。決定論的な合成OOMは、保持された全履歴クローンがユーザーログと同じV8スタックファミリーで失敗しうることを依然として証明しており、一方、新しい `GeminiChat` レッド/グリーンテストは、実際の本番リクエストセットアップパスの1つがそのクローンポイントに到達しなくなったことを証明しています。チェックポイント/再開および圧縮内部処理は、耐久性のあるコピー履歴を正当に必要とする可能性があるため、個別の長期実行検証が依然として必要です。

検証コマンド:

| コマンド                                                                                               | 結果                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `npx vitest run src/core/geminiChat.test.ts`                                                           | 成功、89テスト                                                                                                                            |
| `npx vitest run src/utils/nextSpeakerChecker.test.ts --coverage=false`                                 | 成功、13テスト                                                                                                                            |
| `npx vitest run src/tools/agent/agent.test.ts --coverage=false`                                        | 成功、77テスト                                                                                                                            |
| `npx vitest run --config ./scripts/tests/vitest.config.ts scripts/tests/memory-pressure-repro.test.js` | 成功、1テスト                                                                                                                              |
| `npm run build --workspace=packages/core`                                                              | 成功                                                                                                                                      |
| `npm run build --workspace=packages/cli`                                                               | 成功                                                                                                                                      |
| `npm run typecheck --workspace=packages/core`                                                          | 成功                                                                                                                                      |
| `npm run typecheck --workspace=packages/cli`                                                           | 成功                                                                                                                                      |
| `npm run bundle`                                                                                       | 成功                                                                                                                                      |
| `npm run build`                                                                                        | `packages/vscode-ide-companion` の lint が既存の内部モジュールインポートルールで失敗; 上記の core、CLI、bundle、および対象テストは成功 |

このワークツリーでは、`vscode-ide-companion` パッケージが既存の `import/no-internal-modules` lint エラーに引っかかったため、ルートの `npm run build` は完全にはクリーンではありませんでした。ローカルランタイムテストに必要な core/CLI ビルドと bundle は正常に完了しました。

その後、MCP と hooks を無効にした一時的な設定で、同一の PR レビュープロンプトが実行されました。両方の行は、レビューが完全に終了するのを待つ代わりに、制限された長期実行ウィンドウの後に中断されました。**注意**: この2回の実行はワークロードサイズ（79Kトークン vs 390Kトークン）が交絡しており、制御された実験として比較することはできません。この比較は方向性のある証拠を示すにすぎません。

| バリアント             | 実行時間 | MCPサーバー | ツール | アシスタントメッセージ | ツール使用/結果ブロック | 親ツールID | 合計トークン数 | 最大入力トークン数 | ルート最大RSS |
| -------------------- | ------: | ----------: | ----: | -------------------: | ---------------------: | ----------: | -------------: | -----------------: | -----------: |
| 修正前              | 365.08s |           0 |    19 |                   42 |                42 / 42 |           3 |       79,439 |             26,807 |    357.7 MiB |
| 修正後              | 404.52s |           0 |    19 |                   58 |                52 / 42 |           2 |      390,339 |             54,000 |    310.5 MiB |

これは決定的な同一条件のモデルベンチマークではありません。パッチ適用後の実行はより多くの作業を行い、手動で打ち切られるまでにかなり多くの合計トークンを消費しました。有益なシグナルはより狭いものです。MCPを無効にしたレビューケースで、より多くの作業が観測された場合でも、ルート最大RSSは増加せず、約47.2 MiB低くなりました。これは修正の方向性を支持しますが、長期タスクOOMクラス全体が修正されたことを証明するものではありません。

次に調査すべき残りの高リスククローン/保持パス:

1. 圧縮は依然として要約前に完全な `getHistory(true)` を呼び出します。ヒープがすでに高い場合、圧縮試行によりOOMを引き起こすピークが発生する可能性があります。
2. チェックポイント作成は、元の履歴、クローンされたクライアント履歴、およびシリアル化されたチェックポイントペイロードを同時に保持する可能性があります。
3. フォークサブエージェントは依然として親履歴から `getHistory(true)` でシードされます。
4. ACP/履歴エクスポート/要約/コピーパスは依然として完全な `getHistory()` を呼び出しており、通常のレビューループとは別に監査する必要があります。

バージョンタイミング:

| Issue  | 作成日      | 報告バージョン               | 兆候                                   |
| ------ | ---------- | ------------------------ | ---------------------------------------- |
| #2128  | 2026-03-05 | 指定なし                   | 長期セッションのUIメモリ増加            |
| #2562  | 2026-03-21 | 指定なし                   | 長期セッションでの `structuredClone` OOM   |
| #2868  | 2026-04-03 | 0.13.2                   | ヒープOOM                                 |
| #2945  | 2026-04-07 | 0.14.0                   | V8ヒープOOM                              |
| #4116  | 2026-05-13 | 0.15.11                  | structured-clone スタイルの分析によるOOM |
| #4134  | 2026-05-14 | 0.15.11                  | OOM                                      |
| #4149  | 2026-05-14 | 0.15.10-nightly.20260513 | V8ヒープOOM                              |
| #4167  | 2026-05-15 | 0.15.11                  | 圧縮付近でクラッシュ                   |
| #4185  | 2026-05-15 | 0.15.11                  | トークン圧縮前のヒープ圧力    |
| #4254  | 2026-05-17 | 指定なし                   | メモリが上昇し続ける                      |
| #4276  | 2026-05-18 | 0.15.11                  | V8ヒープOOM                              |
| #4309  | 2026-05-19 | 0.15.11                  | 約7 GiBでの高メモリ警告         |

Issue履歴は、0.15.10がOOMクラスを導入したことを証明していません。同様の報告は3月と4月にも存在しました。ただし、2026-05-13頃から始まり、`v0.15.10`/`v0.15.11` リリースと重なる最近のクラスターを裏付けています。`v0.15.9` と `v0.15.10` の間の関連する差分は、サブエージェントランタイム、非対話型実行、`GeminiChat`、および圧縮コードに大きく影響しているため、この範囲は最初の二分探索ウィンドウとして妥当です。

## 備考

- 最初のコードナビゲーションプロンプトは自由な探索を許可しており、`maxSessionTurns` に達しました; 上記の成功行は制約されたコマンドリストを使用しています。
- 最初のシンセティックdiffの試行では、一時リポジトリ内から相対バンドルパスを使用しました; これらは即座に失敗し、テーブルから除外されています。成功行は絶対ローカルバンドルパスを使用しています。
- 生のJSONLストリームは、プロンプト、ツールコマンド、ツール出力を含むためコミットされていません。レポートには集計診断のみが含まれています。