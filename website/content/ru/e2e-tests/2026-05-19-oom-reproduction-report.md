# Отчёт об OOM-стресс-тестировании и воспроизведении длительных задач

**Дата**: 2026-05-19
**Ветка**: `codex/memory-diagnostics-local-run`
**Тестировал**: yiliang114
**Вывод**: Успешно воспроизведено и найдена первопричина. В версии v0.15.7 (#3735) введённый auto-compaction многократно увеличил частоту вызовов `structuredClone`, что при высоком давлении на heap привело к положительной обратной связи и OOM. Реальные debug-логи полностью подтверждают этот механизм.

---

## 1. Предыстория

Несколько issue (#4309, #4276, #4185, #4315, #4322, #2868) сообщают о V8 heap OOM в qwen-code при длительных сессиях:

```
FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory
```

Характеристики крашей, сообщённых пользователями:
| Issue | Heap при краше | Время работы | Платформа |
|-------|----------------|--------------|-----------|
| #4276 | 4014 MB | ~110 мин | Linux x64 |
| #4315 | 2027 MB | ~19.6 ч | macOS (2GB по умолчанию) |
| #4322 | 4023 MB | ~7 ч | Windows |
| #2868 | 2035 MB | ~1.7 мин | Linux |
| #4309 | 7020 MB | неизвестно | Windows (установлен лимит 8GB, всё равно краш) |

---

## 2. Исправление методологии

Данный отчёт различает два типа тестов:

1. **Тесты с низким давлением на heap**: уменьшение `--max-old-space-size` для усиления проблемы и быстрой локализации кодового пути «копирование целого сегмента при большом history, вызывающее мгновенный пик». Это диагностический инструмент, он не эквивалентен воспроизведению пользовательского OOM на 4G/8G.
2. **Воспроизведение длительных задач с heap по умолчанию**: без установки `NODE_OPTIONS`, восстановление из реального JSONL-истории и продолжение выполнения задачи review с внешней выборкой process-tree RSS. Только такие результаты используются для оценки реального объёма памяти на стороне пользователя.

Следовательно, результаты теста с низким heap не могут сами по себе служить доказательством, что «реальный OOM исправлен». Они лишь показывают, что при достаточно большом history определённый путь может создавать пиковое увеличение, что требует проверки с помощью воспроизведения длительных задач с heap по умолчанию.

## 3. Условия теста с низким давлением на heap

| Параметр | Значение |
| ------------------------ | ------------------------------------------------------------ |
| Версия CLI | 0.15.11 (собрано из ветки `codex/memory-diagnostics-local-run`) |
| Модель | `qwen3.6-plus` (128K контекстное окно) |
| Лимит heap | `--max-old-space-size=512` |
| Предохранитель давления на heap | **Отключён** (HEAP_PRESSURE_COMPRESSION_RATIO установлен в 99.0) |
| Режим работы | YOLO + автоматические многораундовые задачи чтения файлов |
| Рабочий каталог | qwen-code monorepo (3538 .ts файлов, 1.26M строк) |

### Ключевые изменения конфигурации

В `packages/core/src/core/geminiChat.ts` порог compaction по давлению heap изменён с 0.7 на 99.0 (чтобы он никогда не срабатывал), имитируя состояние до исправления #4186.

---

## 4. Результаты теста с низким давлением на heap

### Временная шкала краша

```
[21:26:59] #1 RSS:193.6MB Ctx:0%   → Чтение geminiChat.ts (1500 строк)
[21:27:46] #2 RSS:270.4MB Ctx:4.2% → Чтение agent.ts
[21:28:32] #3 RSS:397.5MB Ctx:4.3% → grep + Чтение 3 файлов
[21:29:18] #4 RSS:452.7MB Ctx:5.7% → Чтение slashCommandProcessor.ts
[21:30:04] #5 RSS:515.0MB Ctx:5.9% → Чтение chatCompressionService.ts
[21:30:50] #6 RSS:649.1MB Ctx:4.0% ← Сработал TOKEN COMPACTION (5.9%→4.0%)
                                       RSS, наоборот, вырос на 134MB (пик structuredClone)
[21:31:36] #7 RSS:666.7MB Ctx:3.2% ← Ещё один compaction, RSS продолжает расти
[21:32:22] CRASH — FATAL ERROR: Ineffective mark-compacts near heap limit
```

**Общее время**: ~5.5 минут, краш после 7 раундов задач.

Это доказывает, что при ограниченном heap длинный history + compaction/клонирование history может вызвать OOM V8 heap.
Однако эти результаты не означают, что реальный пользовательский OOM при heap по умолчанию полностью воспроизведён.

### Синтетическое воспроизведение с большим heap

Чтобы не полагаться только на результаты с 512 MiB, был проведён дополнительный синтетический тест с runtime-давлением на большем heap. Этот тест не вызывает модель, а конструирует историю, аналогичную длительным задачам review/subagent:

- root review turns: 10
- subagent calls: 30
- subagent transcript records: 780
- retained tool result bytes: 193,986,560
- serialized history bytes: 195,620,061
- режим давления: удержанные копии `structuredClone(history)`

| Лимит heap | Давление копий | Результат | Ключевые GC / stack |
| ---------- | --------------: | --------- | ------------------------------------------------------------ |
| 2 GiB | 8 удержанных копий | Не упал, RSS 2.42 GiB, heap used 1.87 GiB | близок к лимиту heap |
| 2 GiB | 10 удержанных копий | OOM | `Reached heap limit`, `ValueDeserializer`, `StructuredClone` |
| 4 GiB | 20 удержанных копий | OOM | `Reached heap limit`, `ValueDeserializer`, `StructuredClone` |

Выдержка из GC при 2 GiB:

```
Mark-Compact 2042.9 (2081.9) -> 2042.9 (2081.1) MB
Mark-Compact 2048.9 (2087.2) -> 2048.9 (2087.2) MB
FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory
...
node::worker::(anonymous namespace)::StructuredClone
```

Выдержка из GC при 4 GiB:

```
Mark-Compact 4082.5 (4126.8) -> 4082.5 (4126.3) MB
Mark-Compact 4095.1 (4139.0) -> 4095.1 (4139.0) MB
FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory
...
node::worker::(anonymous namespace)::StructuredClone
```

Эта группа результатов ближе к сообщениям пользователей о OOM при 2 GiB / 4 GiB: если в истории сохранено достаточно больших tool result / subagent transcript, то удержанное или мгновенное клонирование всего сегмента history может вызвать OOM V8 при heap 2–4 GiB. Это всё ещё синтетическое воспроизведение, не эквивалентное полному бизнес-воспроизведению длительных задач, но оно напрямую доказывает, что проблема не является искусственно созданной малым heap.

### Состояние GC при краше

```
[41381:0x130008000] 342468 ms: Mark-Compact 508.6 (526.7) -> 507.0 (526.9) MB,
  pooled: 1 MB, 86.42 / 0.00 ms  (average mu = 0.175, current mu = 0.150)
  task; scavenge might not succeed

[41381:0x130008000] 342568 ms: Mark-Compact 509.1 (526.9) -> 507.1 (528.2) MB,
  pooled: 0 MB, 93.79 / 0.12 ms  (average mu = 0.121, current mu = 0.068)
  allocation failure; scavenge might not succeed

FATAL ERROR: Ineffective mark-compacts near heap limit
Allocation failed - JavaScript heap out of memory
```

Mark-Compact может освободить только 1–2 MB (почти все объекты достижимы), что доказывает, что память действительно занята законно удерживаемыми объектами.

---

## 5. Воспроизведение длительных задач с heap по умолчанию

Чтобы избежать чрезмерного обобщения результатов с низким heap, было проведено дополнительное воспроизведение с реальным JSONL и heap по умолчанию:

- `NODE_OPTIONS` не устанавливались
- Внутренний профилировщик времени выполнения не включался, чтобы избежать влияния сэмплера на heap
- Каждый CLI создаёт свежую сессию из той же перемотанной JSONL-копии
- Используется временный `QWEN_HOME`, отключены MCP и хуки, чтобы избежать загрязнения локальными глобальными конфигурациями
- Используется только внешняя выборка process-tree RSS

| CLI | Результат | Длительность | Пик Tree RSS | Пик Root RSS | Пик Worker RSS | Примечание |
| -------------------- | ---- | -----: | ------------: | ------------: | --------------: | ----------------------------------------------------------- |
| установленный `qwen` | Успех | 167.3s | 838.0 MiB | 230.2 MiB | 566.3 MiB | Первый свежий запуск столкнулся с ошибкой сервера модели, не включён в вывод; повторный запуск успешен |
| локально пересобранный bundle | Успех | 106.3s | 527.5 MiB | 182.1 MiB | 345.4 MiB | Включает исправление горячего пути клонирования |

Выводы из воспроизведения с heap по умолчанию:

1. Данный JSONL-файл review стабильно даёт от нескольких сотен MiB до ~0.8 GiB process-tree RSS, но OOM на 4G/8G не воспроизводится.
2. Локально пересобранный bundle показывает более низкий пик при том же воспроизведении, что говорит о реальном выигрыше от сокращения горячего пути клонирования history.
3. Это ещё не доказывает, что все пользовательские OOM исправлены. Реальный OOM на 4G/8G всё ещё требует более длительных задач, большего накопления tool-result или воспроизведения с сохранением давления MCP/tool schema.

## 6. Анализ первопричины

### Трёхуровневый механизм OOM

```
┌─────────────────────────────────────────────────────────┐
│ Уровень 3: Лимит V8 Heap (512MB/2GB/4GB)               │ ← Пользователь в итоге упирается сюда
├─────────────────────────────────────────────────────────┤
│ Уровень 2: Пик structuredClone() (мгновенное ~2x)      │ ← Непосредственная причина
├─────────────────────────────────────────────────────────┤
│ Уровень 1: Накопление tool result в истории (линейный рост)│ ← Базовый рост
├─────────────────────────────────────────────────────────┤
│ Уровень 0: Момент срабатывания compaction токенов       │ ← Точка контроля
└─────────────────────────────────────────────────────────┘
```

### Точный путь краша

```
sendMessage()
  → tryCompress()
    → heapPressureRatio < threshold (safety net disabled)
    → ChatCompressionService.compress()
      → chat.getHistory(true)
        → structuredClone(this._history)   ← Пиковое выделение!
          → V8 требуется дополнительно ~N MB для размещения клона
          → Если existing heap + N > limit → OOM
```

### Ключевые доказательства

| Наблюдение | Значение |
| --------------------------------------- | ---------------------------------------------- |
| Задача #5→#6: Context 5.9%→4.0% (снизился) | Token compaction **успешно выполнен** |
| Задача #5→#6: RSS 515→649 MB (вырос на 134MB) | `structuredClone` во время compaction создал пик |
| GC может освободить только 1–2 MB | Все объекты живые (и history, и клон) |
| #4309: краш даже с лимитом 8GB | При достаточно большом history пик клона может превысить любой лимит |

Важно: приведённые выше доказательства основаны на комбинации тестов с низким heap и наблюдений из issue. Воспроизведение с heap по умолчанию пока подтверждает, что «горячий путь клонирования значительно влияет на пиковый RSS», но отдельно OOM на 4G/8G пока не воспроизведён.

### Почему 128K контекстное окно легче вызывает проблему

- 128K × 70% = ~90K токенов, что запускает compaction
- Большое контекстное окно (1M) × 70% = 700K токенов — почти никогда не запускает
- **Чем чаще compaction → тем чаще structuredClone → тем выше риск OOM**
- DeepSeek и другие модели без настройки contextWindowSize по умолчанию имеют 128K, что делает их более уязвимыми

---

## 6.5. Подтверждение реальными журналами работы

Ниже приведены выдержки из debug-вывода локальной сессии краша. Чтобы не раскрывать локальные пути и идентификатор сессии, отчёт содержит только временную шкалу и ключевое содержимое логов.

Сессия запущена в `2026-05-19T13:26:35Z` (по местному времени 21:26:35), краш произошёл в `2026-05-19T13:32:10Z` (по местному времени 21:32:10).

### Временная шкала событий давления на heap и авто-compaction

```
13:29:43 [WARN]  Давление на heap составляет 74.9%; попытка авто-compaction до достижения порога токенов.
13:30:06 [DEBUG] [FILE_READ_CACHE] clear after auto tryCompress    ← compaction #1 выполнен успешно
13:30:13 [WARN]  Давление на heap составляет 70.7%; попытка авто-compaction до достижения порога токенов.
                 ← Сразу после сжатия heap снизился с 74.9% только до 70.7%, всё ещё выше порога, немедленная повторная попытка
13:30:52 [DEBUG] Давление на heap составляет 86.0%; пропускаем авто-compaction по давлению heap из-за периода охлаждения.
                 ← Отказано в выполнении в течение 30-секундного периода охлаждения
13:30:56 [WARN]  Давление на heap составляет 85.3%; попытка авто-compaction до достижения порога токенов.
                 ← Период охлаждения истёк, heap уже вырос до 85.3%
13:31:21 [DEBUG] [FILE_READ_CACHE] clear after auto tryCompress    ← compaction #2 выполнен успешно
13:31:37 [WARN]  Давление на heap составляет 88.8%; попытка авто-compaction до достижения порога токенов.
                 ← После сжатия heap отскочил до 88.8%
13:32:09 [DEBUG] Давление на heap составляет 90.2%; пропускаем авто-compaction по давлению heap из-за периода охлаждения.
                 ← Heap уже достиг 90.2%, выполнение невозможно из-за периода охлаждения
13:32:10 ← Конец лога (процесс упал с OOM)
```

### Интерпретация журнальных данных

| Наблюдение | Значение |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| В течение 2.5 минут было **4** попытки авто-compaction по давлению heap (ещё 2 отклонены из-за периода охлаждения) | `tryCompress`, введённый в #3735, часто срабатывает при высоком давлении |
| После каждого compaction процент heap всё равно >70% | Временный пик от `structuredClone()` сводит на нет выгоду от сжатия |
| 74.9% → 70.7% → 86% → 85.3% → 88.8% → 90.2% → краш | Цикл положительной обратной связи: сжатие → пик клона → heap выше → повторное сжатие → ещё выше |
| Лог обрывается через 1 секунду после 90.2% | Следующий вызов `getHistory(true)` с `structuredClone()` мгновенно превысил лимит |
| `[FILE_READ_CACHE] clear after auto tryCompress` появляется 2 раза | Подтверждает, что compaction прошёл полный путь compress → setHistory |

### Механизм цикла положительной обратной связи

```
Высокий процент heap (>70%)
  → Запускается авто-compaction по давлению heap
    → Внутри tryCompress() вызывается getHistory(true)
      → structuredClone(this._history)  ← Мгновенный пик heap +30~40%
        → Compaction успешен, старый history освобождён
          → Но пик клона уже поднял heap до более опасного уровня
            → Следующий send продолжает накопление
              → Процент heap ещё выше → ещё чаще срабатывает → краш
```

---

## 6.6. Версионная привязка: почему количество OOM-сообщений увеличилось между версиями 0.15.7 и 0.15.11

### Временная шкала ключевых коммитов

| Версия | PR | Изменение | Влияние на частоту вызовов `structuredClone` |
| ------------ | ---------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------- |
| **v0.15.6** | — | `getHistory(true)` вызывается только 1 раз при входе в `sendMessage` | Базовый уровень: 1 клон на send |
| **v0.15.7** | **#3735** `auto-compact subagent context` | `tryCompress()` опущен в `GeminiChat`, выполняется **перед каждым send** | **+1 вызов**: проверка compress перед send |
| **v0.15.10** | **#3879** `reactive compression on context overflow` | Когда провайдер возвращает context overflow, снова запускается `tryCompress()` + `getHistory(true)` | **+1~2 вызова**: путь повторных попыток при overflow |
| **v0.15.10** | **#3985** `harden reactive compression` | Усилена логика повторных попыток реактивного сжатия | То же, что и выше |

### Сравнение точек вызова `getHistory(true)` в v0.15.6 и v0.15.11

**v0.15.6** (2 места):

```
L367: const requestContents = this.getHistory(true);          ← send формирует request
L618: const recoveryContents = self.getHistory(true);         ← эскалация MAX_TOKENS (срабатывает редко)
```

**v0.15.11** (5 мест):

```
L467: внутри ChatCompressionService.compress()                 ← #3735: авто-compact перед каждым send
L574: requestContents = this.getHistory(true);                 ← send формирует request
L724: внутри reactive tryCompress()                           ← #3879: повторная попытка после context overflow
L739: requestContents = self.getHistory(true);                 ← #3879: повторная попытка формирует новый request
L943: const recoveryContents = self.getHistory(true);          ← эскалация MAX_TOKENS
```

### Наихудший путь: один send может вызвать 4 вызова `structuredClone`

```
sendMessage()
  → tryCompress()              ← #3735: getHistory(true) [клон #1]
  → getHistory(true)           ← формирование request [клон #2]
  → API вернул context overflow
    → reactive tryCompress()   ← #3879: getHistory(true) [клон #3]
    → getHistory(true)         ← повторный request [клон #4]
```

### Вывод

**#3735 (v0.15.7)** является наиболее вероятным триггером (не единственной первопричиной) значительного увеличения частоты OOM — он приводит к тому, что каждый `sendMessage` сначала выполняет `tryCompress()`, а внутри `tryCompress` через `ChatCompressionService.compress()` → `chat.getHistory(true)` выполняется полное `structuredClone`. При большом history такой дизайн «сначала клонировать, потом решать, нужно ли сжимать» увеличивает пик памяти с ~1.3x до ~2x+. Примечание: issue history показывает, что сообщения об OOM существовали и до #3735, но #3735 существенно увеличил частоту вызовов `structuredClone`, тем самым значительно повысив вероятность OOM.

**#3879 (v0.15.10)** ещё больше усугубил проблему — когда heap уже на границе (провайдер вернул context overflow), запускается ещё одно полное клонирование, что делает и без того опасную сессию ещё более склонной к крашу.

---

## 7. Проверка эффективности исправления #4186 (сравнительный тест)

Сравнительный тест с включённым предохранителем давления heap (HEAP_PRESSURE_COMPRESSION_RATIO = 0.7):

| Показатель | Предохранитель отключён | Предохранитель включён |
| --------------- | ------------------ | ------------------------- |
| Наличие OOM | Да (краш после 7 раундов) | Нет (успешно работает >10 минут) |
| Пик RSS | 666 MB → краш | 555 MB → GC снизил до 280 MB |
| Срабатывание compaction | Только по порогу токенов | Запускается заранее при 70% heap |
| Поведение контекста | 5.9%→4.0%→краш | 22.7%→17.0% (безопасное снижение) |

**Вывод**: Предохранитель давления heap из #4186 эффективно предотвращает OOM, но это **смягчение**, а не устранение коренной причины.

- Если history уже занимает 60%+ heap, даже преждевременный compaction может не спасти — пик клона всё равно превысит лимит.
- Это объясняет, почему пользователь #4309 с лимитом 8GB всё равно упал.

---

## 8. Распределение памяти

Оценка на основе паттернов роста RSS в тестах:

| Область памяти | Доля | Характер роста |
| -------------------------------- | ------ | --------------------------- |
| `this._history[]` (tool results) | 40–50% | Линейное накопление, +30–100MB за раунд |
| Временная копия `structuredClone()` | 30–40% | Мгновенный пик, появляется при compaction |
| V8 runtime (GC metadata, code) | ~15% | Почти постоянная |
| UI/логирование/буферы потоков | ~5% | Медленный рост |

---

## 9. Скрипт воспроизведения и окружение

### Автоматизированный скрипт-драйвер

```bash
#!/bin/bash
# /tmp/oom-simple-driver.sh <tmux-session-name>
SESSION="$1"

TASKS=(
  "Прочитать полностью с помощью Read-инструмента packages/core/src/core/geminiChat.ts"
  "Прочитать полностью с помощью Read-инструмента packages/core/src/tools/agent/agent.ts"
  "Выполнить grep -rn structuredClone packages/core/src, затем Read первые 3 файла"
  "Прочитать полностью с помощью Read-инструмента packages/cli/src/ui/hooks/slashCommandProcessor.ts"
  "Прочитать полностью с помощью Read-инструмента packages/core/src/services/chatCompressionService.ts"
  "Выполнить find packages/cli/src/ui/commands -name '*.ts', затем Read каждый по очереди"
  "Прочитать полностью с помощью Read-инструмента packages/core/src/core/turn.ts"
  # ... ещё задачи
)

i=0
while true; do
  TASK="${TASKS[$((i % ${#TASKS[@]}))]}"
  i=$((i + 1))

  QWEN_PID=$(ps aux | grep "dist/index.js" | grep -v grep | awk '{print $2}' | sort -rn | head -1)
  RSS=$(ps -o rss= -p $QWEN_PID 2>/dev/null)
  [ -z "$RSS" ] && { echo "CRASH after $((i-1)) tasks!"; exit 0; }

  RSS_MB=$(echo "scale=1; $RSS/1024" | bc)
  CTX=$(tmux capture-pane -t "$SESSION:1" -p 2>/dev/null | grep -oE "[0-9]+\.[0-9]+% 已用" | tail -1)
  echo "[$(date +%H:%M:%S)] #$i RSS:${RSS_MB}MB Ctx:$CTX | ${TASK:0:55}"

  tmux send-keys -t "$SESSION:1" C-u
  sleep 0.2
  tmux send-keys -t "$SESSION:1" "$TASK" Enter
  sleep 0.5
  tmux send-keys -t "$SESSION:1" Enter
  sleep 45
done
```

### Команда запуска

```bash
# 1. Отключить предохранитель давления heap
# geminiChat.ts: HEAP_PRESSURE_COMPRESSION_RATIO = 99.0

# 2. Сборка
npm run build --workspace=packages/core && npm run build --workspace=packages/cli

# 3. Запуск qwen (модель с 128K контекстом, heap 512MB)
SESSION="oom-test"
tmux new-session -d -s "$SESSION" -c "$REPO_DIR"
tmux send-keys -t "$SESSION" \
  "NODE_OPTIONS='--max-old-space-size=512' node packages/cli/dist/index.js --model 'qwen3.6-plus'" Enter

# 4. Дождаться запуска и запустить драйвер
sleep 10
bash /tmp/oom-simple-driver.sh "$SESSION"
```

---

## 10. Рекомендации на будущее

### Краткосрочные смягчения (уже реализованы)

- [x] #4186: предохранитель авто-compaction по давлению heap (порог 0.7)
- [x] #4188: лимиты fileReadCache / crawlCache

### Среднесрочные исправления (предлагаемые)

- [ ] Сократить вызовы `structuredClone()` — `nextSpeakerChecker` нуждается только в последнем сообщении, не нужно клонировать весь history
- [ ] В compaction использовать срез + ссылки вместо полного глубокого клонирования
- [ ] Большие tool result (>100KB) записывать во временные файлы, в history хранить только ссылку с кратким содержанием

### Долгосрочные направления

- [ ] Выгрузка tool result на диск + ленивая загрузка (#4184)
- [ ] Многоуровневая стратегия сжатия на основе RSS (не только количества токенов)
- [ ] Сегментированное хранение history, чтобы избежать однократных полных операций