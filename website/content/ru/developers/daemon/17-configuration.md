# Справочник по конфигурации

## Обзор

На этой странице собраны все настройки, влияющие на работу демона `qwen serve` и его адаптеров: переменные окружения, флаги CLI, ключи `settings.json` и программные параметры. Страницы, посвящённые отдельным функциям, дают ссылку сюда, когда требуются сквозные детали конфигурации.

## Флаги CLI (`qwen serve`)

| Флаг                                    | Тип                        | По умолчанию                                | Эффект                                                                                                                                                                                                                                                                                |
| --------------------------------------- | -------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--hostname <host>`                     | строка                     | `127.0.0.1`                                 | Адрес привязки. Значения петлевого интерфейса: `127.0.0.1`, `localhost`, `::1`, `[::1]`. Для не-петлевого требуется токен Bearer при запуске. Ввод вида `host:port` отклоняется с подсказкой использовать `--port`.                                                                     |
| `--port <n>`                            | число                      | `4170`                                      | Порт для прослушивания; `0` означает временный.                                                                                                                                                                                                                                       |
| `--token <s>`                           | строка                     | переменная окружения                        | Токен Bearer. Переопределяет `QWEN_SERVER_TOKEN` и обрезается при запуске. Отображается в командной строке процесса, поэтому в развёртываниях предпочтительна переменная окружения.                                                                                                    |
| `--require-auth`                        | булево                     | `false`                                     | Расширяет аутентификацию Bearer на петлевой интерфейс и `/health`; при запуске отказывается работать без токена.                                                                                                                                                                      |
| `--workspace <dir>`                     | абсолютный путь            | `process.cwd()`                             | Привязанное рабочее пространство. Должно быть абсолютным путём и каталогом; канонизируется один раз при запуске.                                                                                                                                                                      |
| `--max-sessions <n>`                    | число                      | `20`                                        | Лимит активных сессий. `0` / `Infinity` — без лимита; `NaN` / отрицательные значения вызывают ошибку.                                                                                                                                                                                 |
| `--max-pending-prompts-per-session <n>` | число                      | `5`                                         | Лимит принятых, но ожидающих/выполняющихся запросов на сессию. Превышение возвращает 503. `0` / `Infinity` — без лимита; отрицательные или нецелые значения вызывают ошибку.                                                                                                           |
| `--max-connections <n>`                 | число                      | `256`                                       | `server.maxConnections` для HTTP-слушателя; `0` / `Infinity` — без лимита.                                                                                                                                                                                                            |
| `--enable-session-shell`                | булево                     | `false`                                     | Включает прямое выполнение `POST /session/:id/shell`. Требуется токен Bearer, и каждый вызов должен содержать `X-Qwen-Client-Id`, привязанный к сессии.                                                                                                                               |
| `--event-ring-size <n>`                 | число                      | `8000`                                      | Размер кольцевого буфера воспроизведения SSE на сессию; мягкий лимит — `1_000_000`.                                                                                                                                                                                                   |
| `--http-bridge`                         | булево                     | `true`                                      | Режим моста первого этапа. `--no-http-bridge` всё равно переключается на http-bridge и выводит сообщение в stderr.                                                                                                                                                                    |
| `--mcp-client-budget <n>`               | положительное целое        | не задано                                   | Устанавливает `WorkspaceMcpBudget.clientBudget` и передаёт его дочернему процессу ACP через `childEnvOverrides`.                                                                                                                                                                       |
| `--mcp-budget-mode <m>`                 | `off` / `warn` / `enforce` | `warn` при заданном бюджете, иначе `off`    | Устанавливает `WorkspaceMcpBudget.mode`; `enforce` требует `--mcp-client-budget`.                                                                                                                                                                                                     |
| `--allow-origin <pattern>`              | повторяемая строка          | не задано                                   | Белый список кросс-доменных источников, заменяющий стандартный запрет CORS. `*` разрешает любой источник, но требует токена.                                                                                                                                                          |
| `--allow-private-auth-base-url`         | булево                     | `false`                                     | Разрешает `/workspace/auth/provider` устанавливать `baseUrl` провайдера аутентификации на localhost/частную сеть; используйте только в доверенной локальной разработке.                                                                                                               |
| `--prompt-deadline-ms <n>`              | положительное целое        | не задано                                   | Серверный лимит времени выполнения запроса в миллисекундах. Таймаут прерывает запрос и возвращает ошибку.                                                                                                                                                                             |
| `--writer-idle-timeout-ms <n>`          | положительное целое        | не задано                                   | Таймаут бездействия для каждого SSE-соединения в миллисекундах. Демон закрывает SSE-соединение, если за это время не было отправлено ни одного события.                                                                                                                               |
| `--channel-idle-timeout-ms <n>`         | неотрицательное целое      | `0`                                         | Как долго держать дочерний процесс ACP активным после закрытия последней сессии. `0` — завершить немедленно.                                                                                                                                                                          |
| `--session-reap-interval-ms <n>`        | неотрицательное целое      | `60000`                                     | Интервал сканирования сборщика сессий; `0` отключает его.                                                                                                                                                                                                                             |
| `--session-idle-timeout-ms <n>`         | неотрицательное целое      | `1800000`                                   | Время ожидания перед удалением отключённой сессии; `0` отключает.                                                                                                                                                                                                                     |
| `--rate-limit` / `--no-rate-limit`      | булево                     | переменная окружения / выключено            | Включает послойное HTTP-ограничение скорости для маршрутов prompt, mutation и read.                                                                                                                                                                                                   |
| `--rate-limit-prompt <n>`               | положительное целое        | `10`                                        | Лимит запросов prompt за окно; требует включённого ограничения скорости.                                                                                                                                                                                                              |
| `--rate-limit-mutation <n>`             | положительное целое        | `30`                                        | Лимит запросов mutation за окно; требует включённого ограничения скорости.                                                                                                                                                                                                            |
| `--rate-limit-read <n>`                 | положительное целое        | `120`                                       | Лимит запросов read за окно; требует включённого ограничения скорости.                                                                                                                                                                                                                |
| `--rate-limit-window-ms <n>`            | целое `>= 1000`            | `60000`                                     | Длина окна ограничения скорости; требует включённого ограничения скорости.                                                                                                                                                                                                            |
| нет флага                               | -                          | -                                           | `QWEN_SERVE_NO_MCP_POOL=1` полностью отключает пул.                                                                                                                                                                                                                                   |

## Переменные окружения

### Читаются `runQwenServe` / Express middleware

| Переменная                           | Эффект                                                                                                                                                                             |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `QWEN_SERVER_TOKEN`                  | Токен Bearer; обрезается при запуске.                                                                                                                                             |
| `QWEN_SERVE_DEBUG`                   | `1` / `true` / `on` / `yes` (без учёта регистра) включает подробные логи в stderr. См. [`19-observability.md`](./19-observability.md).                                             |
| `QWEN_SERVE_NO_MCP_POOL`            | `1` отключает пул транспортных каналов MCP рабочего пространства и возвращается к `McpClientManager` на сессию; возможности перестают объявлять `mcp_workspace_pool` / `mcp_pool_restart`. |
| `QWEN_SERVE_PROMPT_DEADLINE_MS`     | Резервная переменная окружения для `--prompt-deadline-ms`.                                                                                                                         |
| `QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS` | Резервная переменная окружения для `--writer-idle-timeout-ms`.                                                                                                                     |
| `QWEN_SERVE_RATE_LIMIT`             | `1` / `true` включает послойное HTTP-ограничение скорости; флаги CLI `--rate-limit` / `--no-rate-limit` имеют приоритет.                                                           |
| `QWEN_SERVE_RATE_LIMIT_PROMPT`      | Резервная переменная окружения для `--rate-limit-prompt`.                                                                                                                          |
| `QWEN_SERVE_RATE_LIMIT_MUTATION`    | Резервная переменная окружения для `--rate-limit-mutation`.                                                                                                                        |
| `QWEN_SERVE_RATE_LIMIT_READ`        | Резервная переменная окружения для `--rate-limit-read`.                                                                                                                            |
| `QWEN_SERVE_RATE_LIMIT_WINDOW_MS`   | Резервная переменная окружения для `--rate-limit-window-ms`.                                                                                                                       |

### Передаются дочернему процессу ACP через `BridgeOptions.childEnvOverrides`

`runQwenServe` создаёт их для каждого handle, чтобы два демона в одном процессе не конкурировали за `process.env`. Переменные бюджета не являются резервными переменными окружения родительского процесса для `qwen serve`; путь CLI должен генерировать их из `--mcp-client-budget` / `--mcp-budget-mode`.

| Переменная                          | Эффект                                                                                                                 |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `QWEN_SERVE_MCP_CLIENT_BUDGET`   | Строка с положительным целым, потребляемая дочерним процессом ACP через `readBudgetFromEnv()`.                         |
| `QWEN_SERVE_MCP_BUDGET_MODE`     | `off` / `warn` / `enforce`.                                                                                            |
| `QWEN_SERVE_MCP_POOL_TRANSPORTS` | Список разрешённых транспортных каналов, разделённый запятыми; по умолчанию объединены `stdio,websocket`; можно явно включить `http,sse`. |
| `QWEN_SERVE_MCP_POOL_DRAIN_MS`   | Задержка очистки неактивного элемента пула; по умолчанию `30000`, ограничена `1000..600000` мс.                        |

### Читаются SDK / адаптерами

| Переменная                     | Эффект                                                                                         |
| ------------------------------ | ---------------------------------------------------------------------------------------------- |
| `QWEN_DAEMON_URL`       | Базовый URL демона для адаптера TUI CLI, каналов и IDE-компаньона.                             |
| `QWEN_DAEMON_TOKEN`     | Токен Bearer.                                                                                  |
| `QWEN_DAEMON_WORKSPACE` | Переопределяет `cwd`, отправляемый в `POST /session`.                                          |

## Ключи `settings.json`

Демон читает настройки один раз при запуске через `loadSettings(boundWorkspace)` внутри `runQwenServe`. Некорректные настройки возвращаются к значениям по умолчанию через try/catch.

| Ключ                         | Тип                                                               | Эффект                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `policy.permissionStrategy` | `'first-responder' \| 'designated' \| 'consensus' \| 'local-only'` | Устанавливает `BridgeOptions.permissionPolicy`; активное значение отображается в `/capabilities` как `policy.permission`. **Запуск проверяет** через `validatePolicyConfig()` на соответствие `SERVE_CAPABILITY_REGISTRY.permission_mediation.modes`. Неизвестные литералы вызывают `InvalidPolicyConfigError` и явно прерывают запуск.                                                                                                                                                                                                |
| `policy.consensusQuorum`    | положительное целое                                               | N для политики `consensus`. **По умолчанию** — `floor(M/2) + 1` от `votersAtIssue.size` (M=2 означает единогласие; большее чётное M — более половины). Если задано при политике, отличной от `consensus`, игнорируется, и при запуске выводится предупреждение в stderr. Неположительные целые вызывают `InvalidPolicyConfigError`. См. [`04-permission-mediation.md`](./04-permission-mediation.md).                                                                                                                                 |
| `context.fileName`          | строка                                                             | Переопределяет `getCurrentGeminiMdFilename()` через `BridgeOptions.contextFilename`.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tools.disabled`            | string[]                                                           | Инструменты, отключённые для следующего запуска дочернего процесса ACP. Нормализуются через `normalizeDisabledToolList()` (`packages/cli/src/config/normalizeDisabledTools.ts`): не-массив становится `[]`, нестроковые записи пропускаются, пробелы обрезаются, пустые записи удаляются, дубликаты удаляются с сохранением первого вхождения. Как при запуске, так и при обновлении настроек `restartMcpServer` используется эта функция. `ToolRegistry.has(name)` чувствителен к регистру и точен. `POST /workspace/tools/:name/enable` и `tool_toggled` обновляют этот ключ. |
| `tools.approvalMode`        | `'default' \| 'auto' \| ...`                                       | Режим утверждения по умолчанию для сессии; `POST /session/:id/approval-mode` записывает сюда при `persist: true`.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `telemetry`                 | объект                                                             | Конфигурация OTel. Ключи включают `enabled`, `otlpEndpoint`, `otlpProtocol`, `otlpTracesEndpoint`, `otlpLogsEndpoint`, `otlpMetricsEndpoint`, `target`, `outfile`, `includeSensitiveSpanAttributes`, `sensitiveSpanAttributeMaxLength`, `resourceAttributes` и `metrics.includeSessionId`. `resolveTelemetrySettings()` читает его при запуске и инициализирует `initializeTelemetry()`.                                                                                                                                              |

## `ServeOptions` (программное внедрение)

`packages/cli/src/serve/types.ts` определяет типизированный объект параметров, принимаемый как `runQwenServe`, так и `createServeApp`. Он отражает флаги CLI выше и добавляет:

| Поле                            | Эффект                                                                                        |
| ------------------------------- | --------------------------------------------------------------------------------------------- |
| `eventRingSize`                 | Переопределяет размер кольцевого буфера на сессию по умолчанию.                               |
| `maxPendingPromptsPerSession` | Лимит ожидающих запросов на сессию; `0` / `Infinity` — без лимита.                            |
| `mcpPoolActive`               | Программный переключатель, по умолчанию из `QWEN_SERVE_NO_MCP_POOL`.                          |
| `allowOrigins`                | Белый список кросс-доменных источников (`string[]`), соответствует `--allow-origin`.          |
| `allowPrivateAuthBaseUrl`     | Разрешает установку `baseUrl` частного / локального провайдера аутентификации.                |
| `enableSessionShell`          | Включает выполнение shell сессии; токен Bearer и идентификатор клиента, привязанный к сессии, по-прежнему обязательны. |
| `promptDeadlineMs`            | Лимит времени выполнения запроса.                                                             |
| `writerIdleTimeoutMs`         | Таймаут бездействия SSE-записи.                                                               |
| `channelIdleTimeoutMs`        | Как долго держать дочерний процесс ACP тёплым после закрытия последней сессии.                |
| `sessionReapIntervalMs`       | Интервал сканирования сборщика сессий.                                                        |
| `sessionIdleTimeoutMs`        | Время ожидания перед удалением отключённой сессии.                                            |
| `rateLimit*`                  | Переключатель послойного HTTP-ограничения скорости, пороги и окно.                             |
## `BridgeOptions` (программное встраивание моста)

`packages/acp-bridge/src/bridgeOptions.ts` определяет параметры моста. Полная таблица — в [`03-acp-bridge.md`](./03-acp-bridge.md). Ключевые поля:

| Поле                                                                                                                   | Действие                                                                                     |
| ----------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `boundWorkspace`                                                                                                        | Обязательное каноническое рабочее пространство.                                              |
| `sessionScope`                                                                                                          | `'single'` (по умолчанию) vs `'thread'`.                                                    |
| `initializeTimeoutMs`, `maxSessions`, `eventRingSize`, `permissionResponseTimeoutMs`, `maxPendingPermissionsPerSession | Ограничения по ресурсам.                                                                     |
| `channelFactory`                                                                                                        | Подключаемая фабрика ACP-дочерних процессов; по умолчанию — `defaultSpawnChannelFactory`.    |
| `fileSystem`                                                                                                            | Адаптер `BridgeFileSystem`. См. [`07-workspace-filesystem.md`](./07-workspace-filesystem.md).|
| `permissionPolicy`, `permissionConsensusQuorum`, `permissionAudit`                                                      | Связь с медиатором.                                                                          |
| `statusProvider`                                                                                                        | Предварительные проверки от демона-хоста.                                                    |
| `childEnvOverrides`                                                                                                     | Добавление или удаление переменных окружения для каждого дескриптора.                        |
| `contextFilename`                                                                                                       | Переопределяет `getCurrentGeminiMdFilename()`.                                               |
| `channelIdleTimeoutMs`                                                                                                  | Время (в мс) удержания ACP-дочернего процесса после закрытия последней сессии; по умолчанию `0`. |

## Важные значения по умолчанию

| Константа                        | Файл                    | Значение           | Назначение                                                        |
| -------------------------------- | ----------------------- | ------------------ | ----------------------------------------------------------------- |
| `DEFAULT_MAX_SESSIONS`           | `bridge.ts`             | `20`               | Ограничение сессий до `SessionLimitExceededError`.                |
| `MAX_EVENT_RING_SIZE`            | `bridge.ts`             | `1_000_000`        | Мягкий лимит для `BridgeOptions.eventRingSize`; защита от опечаток. |
| `DEFAULT_RING_SIZE`              | `eventBus.ts`           | `8000`             | Глубина кольцевого буфера SSE-воспроизведения на сессию.          |
| `DEFAULT_MAX_QUEUED`             | `eventBus.ts`           | `256`              | Лимит очереди на подписчика.                                      |
| `DEFAULT_MAX_SUBSCRIBERS`        | `eventBus.ts`           | `64`               | Лимит подписчиков на шину.                                        |
| `WARN_THRESHOLD_RATIO`           | `eventBus.ts`           | `0.75`             | Порог срабатывания `slow_client_warning`.                         |
| `WARN_RESET_RATIO`               | `eventBus.ts`           | `0.375`            | Порог гистерезиса для перезапуска предупреждения.                 |
| `DEFAULT_INIT_TIMEOUT_MS`        | `bridge.ts`             | `10_000`           | Таймаут подтверждения соединения ACP `initialize`.                |
| `MCP_RESTART_TIMEOUT_MS`         | `bridge.ts`             | `300_000`          | Таймаут моста для `/workspace/mcp/:server/restart`.               |
| `DEFAULT_PERMISSION_TIMEOUT_MS`  | `bridge.ts`             | `5 * 60_000`       | Таймаут запроса разрешения (настенные часы).                      |
| `DEFAULT_MAX_PENDING_PER_SESSION`| `bridge.ts`             | `64`               | Соответствует `DEFAULT_MAX_SUBSCRIBERS`.                          |
| `MAX_RESOLVED_PERMISSION_RECORDS`| `permissionMediator.ts` | `512`              | FIFO для недавно обработанных разрешений.                         |
| `KILL_HARD_DEADLINE_MS`          | `spawnChannel.ts`       | `10_000`           | Окно корректного завершения канала.                               |
| `SHUTDOWN_FORCE_CLOSE_MS`        | `run-qwen-serve.ts`       | `5_000`            | Таймер принудительного закрытия HTTP-сервера.                     |
| `MAX_READ_BYTES`                 | `fs/policy.ts`          | `256 * 1024`       | Лимит на чтение.                                                  |
| `MAX_WRITE_BYTES`                | `fs/policy.ts`          | `5 * 1024 * 1024`  | Лимит на запись.                                                  |
| `MAX_DISPLAY_NAME_LENGTH`        | `bridge.ts`             | `256`              | Лимит длины `displayName` сессии.                                 |

## Перекрёстные ссылки

- Настройки аутентификации: [`12-auth-security.md`](./12-auth-security.md)
- Возможности и версия протокола: [`11-capabilities-versioning.md`](./11-capabilities-versioning.md)
- Кольцевой буфер событий и настройка backpressure: [`10-event-bus.md`](./10-event-bus.md)
- Пул MCP / бюджет: [`05-mcp-transport-pool.md`](./05-mcp-transport-pool.md) и [`06-mcp-budget-guardrails.md`](./06-mcp-budget-guardrails.md)
- Политика разрешений: [`04-permission-mediation.md`](./04-permission-mediation.md)
- Руководство пользователя: [`../../users/qwen-serve.md`](../../users/qwen-serve.md)