# Интеграция с IDE

Qwen Code может интегрироваться с вашей средой разработки (IDE) для обеспечения более плавного и контекстно-ориентированного взаимодействия. Эта интеграция позволяет командному интерфейсу (CLI) лучше понимать вашу рабочую область и активирует мощные функции, такие как нативное сравнение изменений прямо в редакторе.

На данный момент единственной поддерживаемой IDE является [Visual Studio Code](https://code.visualstudio.com/) и другие редакторы, поддерживающие расширения VS Code. Чтобы добавить поддержку других редакторов, ознакомьтесь со спецификацией [IDE Companion Extension Spec](../users/ide-integration/ide-companion-spec).

## Возможности

- **Контекст рабочей области:** CLI автоматически получает информацию о вашей рабочей области для предоставления более релевантных и точных ответов. Этот контекст включает:
  - **10 самых последних файлов**, к которым вы обращались в своей рабочей области.
  - Активную позицию курсора.
  - Любой выделенный текст (до лимита 16 КБ; более длинные выделения будут обрезаны).

- **Встроенный просмотр различий (Diff):** Когда Qwen предлагает изменения кода, вы можете просматривать эти изменения непосредственно во встроенном инструменте сравнения вашего IDE. Это позволяет вам легко просматривать, редактировать, а также принимать или отклонять предложенные изменения.

- **Команды VS Code:** Вы можете получить доступ к функциям Qwen Code прямо из палитры команд VS Code (`Cmd+Shift+P` или `Ctrl+Shift+P`):
  - `Qwen Code: Run`: Запускает новый сеанс Qwen Code во встроенном терминале.
  - `Qwen Code: Accept Diff`: Принимает изменения в активном редакторе различий.
  - `Qwen Code: Close Diff Editor`: Отклоняет изменения и закрывает активный редактор различий.
  - `Qwen Code: View Third-Party Notices`: Отображает уведомления о сторонних библиотеках расширения.

## Установка и настройка

Существует три способа настройки интеграции с IDE:

### 1. Автоматическая настройка (рекомендуется)

Когда вы запускаете Qwen Code внутри поддерживаемого редактора, он автоматически определит вашу среду и предложит подключиться. Если вы ответите «Да», автоматически выполнится необходимая настройка, включающая установку соответствующего расширения и активацию соединения.

### 2. Ручная установка через CLI

Если вы ранее отклонили приглашение или хотите установить расширение вручную, вы можете выполнить следующую команду внутри Qwen Code:

```
/ide install
```

Эта команда найдет правильное расширение для вашей IDE и установит его.

### 3. Ручная установка из маркетплейса

Вы также можете установить расширение напрямую из маркетплейса.

- **Для Visual Studio Code:** Установите из [VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=qwenlm.qwen-code-vscode-ide-companion).
- **Для форков VS Code:** Чтобы поддерживать форки VS Code, расширение также опубликовано в [Open VSX Registry](https://open-vsx.org/extension/qwenlm/qwen-code-vscode-ide-companion). Следуйте инструкциям вашего редактора для установки расширений из этого реестра.

> ПРИМЕЧАНИЕ:
> Расширение "Qwen Code Companion" может отображаться в нижней части результатов поиска. Если вы не видите его сразу, попробуйте прокрутить вниз или отсортировать по "Новым публикациям".
>
> После ручной установки расширения необходимо выполнить `/ide enable` в CLI для активации интеграции.

## Использование

### Включение и отключение

Вы можете управлять интеграцией с IDE прямо из CLI:

- Чтобы включить подключение к IDE, выполните:
  ```
  /ide enable
  ```
- Чтобы отключить подключение, выполните:
  ```
  /ide disable
  ```

При включении Qwen Code автоматически попытается подключиться к расширению-компаньону IDE.

### Проверка статуса

Чтобы проверить состояние подключения и посмотреть контекст, который CLI получил от IDE, выполните:

```
/ide status
```

Если подключение установлено, эта команда покажет, к какой IDE выполнено подключение, а также список недавно открытых файлов, о которых известно.

(Примечание: Список файлов ограничен 10 последними открытыми файлами в вашем рабочем пространстве и включает только локальные файлы на диске.)

### Работа с различиями (Diffs)

Когда вы просите модель Qwen изменить файл, она может открыть представление различий непосредственно в вашем редакторе.

**Чтобы принять изменения (diff)**, вы можете выполнить любое из следующих действий:

- Нажать **иконку с галочкой** в заголовке редактора различий.
- Сохранить файл (например, с помощью `Cmd+S` или `Ctrl+S`).
- Открыть палитру команд и выполнить **Qwen Code: Accept Diff**.
- Ответить `yes` в интерфейсе командной строки при запросе.

**Чтобы отклонить изменения (diff)**, вы можете:

- Нажать **иконку 'x'** в заголовке редактора различий.
- Закрыть вкладку редактора различий.
- Открыть палитру команд и выполнить **Qwen Code: Close Diff Editor**.
- Ответить `no` в интерфейсе командной строки при запросе.

Вы также можете **изменить предложенные правки** непосредственно в представлении различий перед их принятием.

Если вы выберете «Yes, allow always» в интерфейсе командной строки, изменения больше не будут отображаться в IDE, так как они будут автоматически приняты.

## Использование в песочнице

Если вы используете Qwen Code в песочнице, обратите внимание на следующее:

- **В macOS:** Для интеграции с IDE требуется доступ к сети для взаимодействия с расширением-компаньоном IDE. Вы должны использовать профиль Seatbelt, который разрешает доступ к сети.
- **В контейнере Docker:** Если вы запускаете Qwen Code внутри контейнера Docker (или Podman), интеграция с IDE все равно может подключиться к расширению VS Code, работающему на вашем хост-компьютере. CLI настроен таким образом, чтобы автоматически находить сервер IDE по адресу `host.docker.internal`. Обычно никакая дополнительная настройка не требуется, но вам может понадобиться убедиться, что настройки сети Docker позволяют установить соединение из контейнера с хостом.

## Устранение неполадок

Если у вас возникли проблемы с интеграцией IDE, ниже приведены некоторые распространенные сообщения об ошибках и способы их устранения.

### Ошибки подключения

- **Сообщение:** `🔴 Отключено: Не удалось подключиться к расширению-компаньону IDE для [IDE Name]. Убедитесь, что расширение запущено, и попробуйте перезапустить терминал. Чтобы установить расширение, выполните /ide install.`
  - **Причина:** Qwen Code не смог найти необходимые переменные окружения (`QWEN_CODE_IDE_WORKSPACE_PATH` или `QWEN_CODE_IDE_SERVER_PORT`) для подключения к IDE. Обычно это означает, что расширение-компаньон IDE не запущено или не было корректно инициализировано.
  - **Решение:**
    1.  Убедитесь, что вы установили расширение **Qwen Code Companion** в вашей IDE и что оно включено.
    2.  Откройте новое окно терминала в вашей IDE, чтобы убедиться, что оно получило правильное окружение.

- **Сообщение:** `🔴 Отключено: Ошибка подключения к IDE. Соединение было неожиданно потеряно. Попробуйте восстановить подключение, выполнив /ide enable`
  - **Причина:** Подключение к расширению-компаньону IDE было потеряно.
  - **Решение:** Выполните `/ide enable`, чтобы попытаться восстановить подключение. Если проблема сохраняется, откройте новое окно терминала или перезапустите вашу IDE.

### Ошибки конфигурации

- **Сообщение:** `🔴 Отключено: Несоответствие каталогов. Qwen Code запущен в другом месте, отличном от открытого рабочего пространства в [IDE Name]. Пожалуйста, запустите CLI из того же каталога, что и корневая папка вашего проекта.`
  - **Причина:** Текущий рабочий каталог CLI находится вне папки или рабочего пространства, открытого в вашей IDE.
  - **Решение:** Перейдите с помощью `cd` в тот же каталог, который открыт в вашей IDE, и перезапустите CLI.

- **Сообщение:** `🔴 Отключено: Чтобы использовать эту функцию, откройте папку рабочего пространства в [IDE Name] и попробуйте снова.`
  - **Причина:** В вашей IDE не открыто ни одно рабочее пространство.
  - **Решение:** Откройте рабочее пространство в вашей IDE и перезапустите CLI.

### Общие ошибки

- **Сообщение:** `Интеграция с IDE не поддерживается в вашей текущей среде. Чтобы использовать эту функцию, запустите Qwen Code в одной из этих поддерживаемых IDE: [Список IDE]`
  - **Причина:** Вы запускаете Qwen Code в терминале или среде, которая не является поддерживаемой IDE.
  - **Решение:** Запустите Qwen Code из встроенного терминала поддерживаемой IDE, например VS Code.

- **Сообщение:** `Для IDE недоступен установщик. Пожалуйста, установите расширение Qwen Code Companion вручную из маркетплейса.`
  - **Причина:** Вы выполнили команду `/ide install`, но у CLI нет автоматического установщика для вашей конкретной IDE.
  - **Решение:** Откройте маркетплейс расширений вашей IDE, найдите "Qwen Code Companion" и установите его вручную.