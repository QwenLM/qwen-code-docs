# Команды

В этом документе подробно описаны все команды, поддерживаемые Qwen Code, которые помогут вам эффективно управлять сеансами, настраивать интерфейс и контролировать его поведение.

Команды Qwen Code активируются через определённые префиксы и делятся на три категории:

| Тип префикса                  | Описание функции                                | Типичный сценарий использования                  |
| ----------------------------- | ----------------------------------------------- | ------------------------------------------------ |
| Команды с косой чертой (`/`)  | Управление Qwen Code на метауровне              | Управление сеансами, изменение настроек, помощь   |
| Команды с «собакой» (`@`)     | Быстрая вставка локального содержимого файлов в диалог | Разрешение ИИ анализировать указанные файлы или код в каталогах |
| Команды с восклицательным знаком (`!`) | Прямое взаимодействие с системной оболочкой    | Выполнение системных команд, таких как `git status`, `ls` и т.д. |

## 1. Команды с косой чертой (`/`)

Команды с косой чертой используются для управления сеансами Qwen Code, интерфейсом и базовым поведением.

### 1.1 Управление сеансом и проектом

Эти команды помогают сохранять, восстанавливать и обобщать результаты работы.

| Команда           | Описание                                                             | Примеры использования                                                  |
| ----------------- | -------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `/init`           | Анализирует текущий каталог и создаёт начальный контекстный файл     | `/init`                                                                |
| `/summary`        | Генерирует сводку проекта на основе истории диалога                  | `/summary`                                                             |
| `/compress`       | Заменяет историю диалога сводкой для экономии токенов                | `/compress` или `/summarize`                                           |
| `/compress-fast`  | Быстрое сжатие без ИИ — удаляет старые выводы инструментов и «размышления» | `/compress-fast`                                                       |
| `/resume`         | Возобновляет предыдущий сеанс диалога                                | `/resume` или `/continue`                                              |
| `/recap`          | Генерирует однострочную сводку текущего сеанса прямо сейчас          | `/recap`                                                               |
| `/restore`        | Откатывает файлы проекта к контрольной точке до выполнения вызова инструмента | `/restore` (список) или `/restore <ID>`                                |
| `/delete`         | Удаляет предыдущий сеанс                                             | `/delete`                                                              |
| `/branch`         | Создаёт ответвление текущего диалога в новый сеанс                   | `/branch`                                                              |
| `/fork`           | Запускает фонового агента, наследуя полный диалог                    | `/fork <директива>`                                                    |
| `/rewind`         | Отматывает диалог к предыдущему шагу                                 | `/rewind` или `/rollback`                                              |
| `/export`         | Экспортирует историю сеанса в файл                                   | `/export html`, `/export md`, `/export json`, `/export jsonl`         |
| `/rename`         | Переименовывает или добавляет тег текущему сеансу                    | `/rename My Feature` или `/tag`                                        |

> [!note]
>
> `/summarize` — это псевдоним для `/compress` (он сжимает историю диалога — деструктивная операция). Чтобы вместо этого создать неразрушающую сводку проекта, используйте `/summary`.

### 1.2 Управление интерфейсом и рабочей областью

Команды для настройки внешнего вида интерфейса и рабочего окружения.

| Команда             | Описание                                                                                                                                                                      | Примеры использования                                                                       |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `/clear`            | Очищает историю диалога и освобождает контекст                                                                                                                                | `/clear`, `/reset`, `/new`                                                                   |
| `/context`          | Показывает разбивку использования контекстного окна                                                                                                                           | `/context`                                                                                   |
| → `detail`          | Показывает разбивку использования контекста по элементам                                                                                                                      | `/context detail`                                                                            |
| `/history`          | Управляет настройками отображения истории и её видимостью                                                                                                                     | `/history collapse-on-resume`, `/history expand-on-resume`, `/history expand-now`            |
| `/diff`             | Открывает интерактивное окно просмотра различий, показывающее неотслеживаемые изменения и изменения по шагам. Используйте ←/→ для переключения между текущим git diff и отдельными шагами диалога, ↑/↓ для просмотра файлов | `/diff`                                                                                      |
| `/theme`            | Изменяет визуальную тему Qwen Code                                                                                                                                            | `/theme`                                                                                     |
| `/vim`              | Включает/выключает режим редактирования Vim в поле ввода                                                                                                                      | `/vim`                                                                                       |
| `/voice`            | Включает/выключает голосовой ввод                                                                                                                                             | `/voice`, `/voice hold`, `/voice tap`, `/voice off`, `/voice status`                         |
| `/directory`        | Управляет многокаталоговой рабочей областью                                                                                                                                   | `/dir add ./src,./tests`, `/dir show`                                                        |
| `/cd`               | Перемещает текущий сеанс в новый рабочий каталог                                                                                                                              | `/cd ../other-project`                                                                       |
| `/editor`           | Открывает диалог выбора поддерживаемого редактора                                                                                                                             | `/editor`                                                                                    |
| `/statusline`        | Открывает интерактивный диалог выбора предустановок [статусной строки](./status-line.md)                                                                                     | `/statusline`                                                                                |
| `/statusline <text>` | Генерирует [статусную строку](./status-line.md) в командном режиме через агента                                                                                              | `/statusline show model and git branch`                                                      |
| `/terminal-setup`    | Настраивает привязки клавиш терминала для многострочного ввода                                                                                                                | `/terminal-setup`                                                                            |

### 1.3 Языковые настройки

Команды для управления языком интерфейса и вывода.

| Команда              | Описание                             | Примеры использования              |
| -------------------- | ------------------------------------ | ---------------------------------- |
| `/language`          | Просмотр или изменение языковых настроек | `/language`                        |
| → `ui [язык]`        | Установить язык интерфейса           | `/language ui zh-CN`               |
| → `output [язык]`    | Установить язык вывода LLM           | `/language output Chinese`         |

- Доступные встроенные языки интерфейса: `zh-CN` (Упрощённый китайский), `en-US` (Английский), `ru-RU` (Русский), `de-DE` (Немецкий), `ja-JP` (Японский), `pt-BR` (Португальский – Бразилия), `fr-FR` (Французский), `ca-ES` (Каталанский).
- Примеры языка вывода: `Chinese`, `English`, `Japanese` и т.д.

### 1.4 Управление инструментами и моделями

Команды для управления инструментами и моделями ИИ.

| Команда         | Описание                                         | Примеры использования                                                                                              |
| --------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| `/mcp`          | Выводит список настроенных MCP-серверов и инструментов | `/mcp`, `/mcp desc`, `/mcp nodesc`, `/mcp schema`                                                                  |
| `/import-config` | Импортирует MCP-серверы из конфигураций Claude   | `/import-config all`, `/import-config claude-code`, `/import-config claude-desktop --scope user\|project`          |
| `/tools`        | Показывает текущий список доступных инструментов | `/tools`, `/tools desc`                                                                                            |
| `/skills`       | Выводит список и запускает доступные навыки      | `/skills`, `/skills <имя>`                                                                                         |
| `/plan`         | Переключается в режим планирования или выходит из него | `/plan`, `/plan <задача>`, `/plan exit`                                                                            |
| `/approval-mode`| Изменяет режим одобрения инструментов (только для текущего сеанса) | `/approval-mode`, `/approval-mode auto-edit`                                                                       |
| → `plan`        | Только анализ, без выполнения (безопасная проверка) | `/approval-mode plan`                                                                                              |
| → `default`     | Требуется одобрение для изменений (повседневное использование) | `/approval-mode default`                                                                                            |
| → `auto-edit`   | Автоматическое одобрение изменений (доверенная среда) | `/approval-mode auto-edit`                                                                                          |
| → `auto`        | Одобрение, оцениваемое классификатором (автономный режим) | `/approval-mode auto`                                                                                              |
| → `yolo`        | Автоматическое одобрение всего (быстрое прототипирование) | `/approval-mode yolo`                                                                                              |
| `/model`        | Переключает модель, используемую в текущем сеансе | `/model`, `/model <model-id>` (немедленное переключение)                                                           |
| `/model --fast` | Устанавливает более лёгкую модель для подсказок   | `/model --fast qwen3-coder-flash`                                                                                  |
| `/model --voice`| Устанавливает модель для распознавания голоса     | `/model --voice <model-id>`                                                                                        |
| `/extensions`   | Управление расширениями                           | `/extensions list`, `/extensions manage`                                                                            |
| → `list`        | Выводит список установленных расширений           | `/extensions list`                                                                                                 |
| → `manage`      | Управление установленными расширениями (интерактивно) | `/extensions manage`                                                                                               |
| → `explore`     | Открывает страницу расширений в браузере           | `/extensions explore <Gemini\|ClaudeCode>`                                                                          |
| → `install`     | Устанавливает расширение из git-репозитория или по пути | `/extensions install <repo-or-path>`                                                                                |
| `/memory`       | Открывает диалог «Диспетчер памяти»               | `/memory`                                                                                                          |
| `/remember`     | Сохраняет долговременное воспоминание              | `/remember Prefer terse responses`                                                                                  |
| `/forget`       | Удаляет соответствующие записи из автоматической памяти | `/forget <запрос>`                                                                                                  |
| `/dream`        | Запускает консолидацию автоматической памяти вручную | `/dream`                                                                                                           |
| `/hooks`        | Управление хуками Qwen Code                       | `/hooks`, `/hooks list`                                                                                            |
| `/permissions`  | Управление правилами разрешений                   | `/permissions`                                                                                                     |
| `/agents`       | Управление подчинёнными агентами                   | `/agents manage`, `/agents create`                                                                                 |
| `/arena`        | Управление сеансами Arena                         | `/arena start`, `/arena stop`, `/arena status`, `/arena select` (псевдоним `choose`)                               |
| `/goal`         | Устанавливает цель – продолжать работу до выполнения условия | `/goal <условие>`, `/goal clear`                                                                                    |
| `/tasks`        | Выводит список фоновых задач                      | `/tasks`                                                                                                           |
| `/workflows`    | Просмотр запусков рабочих процессов               | `/workflows`, `/workflows <runId>`                                                                                 |
| `/lsp`          | Показывает статус LSP-сервера                     | `/lsp`                                                                                                             |
| `/trust`        | Управление настройками доверия к папкам           | `/trust`                                                                                                           |

> [!warning]
>
> Устанавливайте расширения (`/extensions install`) только из источников, которым вы доверяете. Расширения могут включать MCP-серверы, навыки и команды, которые работают с теми же правами, что и Qwen Code, — они могут получить доступ к вашим файлам, API-ключам и данным диалога. `/extensions install` не запрашивает подтверждения.

> [!warning]
>
> Режимы одобрения `auto-edit`, `auto` и `yolo` отключают запросы подтверждения при выполнении инструментов. В режиме `yolo` все действия, включая shell-команды, запись файлов и сетевые запросы, выполняются без подтверждения. Используйте эти режимы только в доверенных, изолированных или одноразовых средах.

> [!note]
>
> `/workflows`, `/lsp` и `/trust` регистрируются только тогда, когда соответствующая функция включена — через переменную окружения `QWEN_CODE_ENABLE_WORKFLOWS=1`, флаг CLI `--experimental-lsp` и настройку `security.folderTrust.enabled` соответственно. Когда они отключены, команды не отображаются и при попытке использования будет выведено сообщение о неизвестной команде.

### 1.5 Встроенные навыки

Эти команды запускают встроенные навыки, предоставляющие специализированные рабочие процессы.

| Команда      | Описание                                                        | Примеры использования                               |
| ------------ | --------------------------------------------------------------- | --------------------------------------------------- |
| `/review`    | Просмотр изменений кода с 5 параллельными агентами + детерминированный анализ | `/review`, `/review 123`, `/review 123 --comment`  |
| `/loop`      | Выполняет запрос по повторяющемуся расписанию                  | `/loop 5m check the build`                          |
| `/simplify`  | Просматривает недавние изменения и применяет безопасные правки по очистке | `/simplify`, `/simplify focus on duplication`      |
| `/qc-helper` | Отвечает на вопросы об использовании и настройке Qwen Code     | `/qc-helper how do I configure MCP?`                |

Полную документацию по `/review` см. в разделе [Проверка кода](./code-review.md).

### 1.6 Побочный вопрос (`/btw`)

Команда `/btw` позволяет задавать быстрые побочные вопросы, не прерывая и не влияя на основной ход диалога.

| Команда                | Описание                          |
| ---------------------- | --------------------------------- |
| `/btw <ваш вопрос>`    | Задать быстрый побочный вопрос    |
| `?btw <ваш вопрос>`    | Альтернативный синтаксис для побочных вопросов |

**Как это работает:**

- Побочный вопрос отправляется как отдельный вызов API с контекстом недавнего диалога (до последних 20 сообщений).
- Ответ отображается над полем ввода — вы можете продолжать печатать, пока ждёте.
- Основной диалог **не блокируется** — он продолжается независимо.
- Ответ на побочный вопрос **не** становится частью истории основного диалога.
- Ответы отображаются с полной поддержкой Markdown (блоки кода, списки, таблицы и т.д.).

**Горячие клавиши (интерактивный режим):**

| Сочетание клавиш     | Действие                                                   |
| -------------------- | ---------------------------------------------------------- |
| `Escape`             | Отменить (во время загрузки) или закрыть (после завершения) |
| `Space` или `Enter`  | Закрыть ответ (когда поле ввода пусто)                     |
| `Ctrl+C` или `Ctrl+D`| Отменить выполняющийся побочный вопрос                     |

**Пример:**

```
(Пока основной диалог посвящён рефакторингу кода)

> /btw Какая разница между let и var в JavaScript?

  ╭──────────────────────────────────────────╮
  │ /btw Какая разница между let и var в     │
  │     JavaScript?                          │
  │                                          │
  │ + Отвечаю...                             │
  │ Нажмите Escape, Ctrl+C или Ctrl+D,       │
  │ чтобы отменить                           │
  ╰──────────────────────────────────────────╯
  > (Поле ввода остаётся активным — продолжайте печатать)

(После получения ответа)

  ╭──────────────────────────────────────────╮
  │ /btw Какая разница между let и var в     │
  │     JavaScript?                          │
  │                                          │
  │ `let` имеет блочную область видимости,   │
  │ а `var` — функциональную. `let` появился │
  │ в ES6 и не «всплывает» так же, как `var`.│
  │                                          │
  │ Нажмите Space, Enter или Escape,         │
  │ чтобы закрыть                            │
  ╰──────────────────────────────────────────╯
  > (Поле ввода всё ещё активно)
```
**Поддерживаемые режимы выполнения:**

| Режим                | Поведение                                                                   |
| -------------------- | --------------------------------------------------------------------------- |
| Интерактивный        | Показывается над Composer с рендерингом Markdown                            |
| Неинтерактивный      | Возвращает текстовый результат: `btw> вопрос\nответ`                        |
| ACP (Agent Protocol) | Возвращает асинхронный генератор stream_messages                            |

> [!tip]
>
> Используйте `/btw`, когда нужен быстрый ответ, не отвлекаясь от основной задачи. Это особенно полезно для уточнения концепций, проверки фактов или получения быстрых пояснений, оставаясь сфокусированным на основном рабочем процессе.

### 1.7 Итоги сессии (`/recap`)

Команда `/recap` генерирует краткий обзор текущей сессии в стиле «на чём вы остановились», чтобы вы могли возобновить старый разговор без прокрутки страниц истории.

| Команда   | Описание                                          |
| --------- | ------------------------------------------------- |
| `/recap`  | Сгенерировать и показать однострочные итоги сессии|

**Как это работает:**

- Использует настроенную быструю модель (параметр `fastModel`), если она доступна, иначе — основную модель сессии. Небольшая дешёвая модель вполне подходит для итогов.
- Последние сообщения разговора (до 30 сообщений, только текст — вызовы инструментов и ответы инструментов отфильтровываются) отправляются модели с кратким системным промптом.
- Итоги отображаются тусклым цветом с префиксом `❯`, чтобы они выделялись на фоне реальных ответов ассистента.
- Команда отказывается с сообщением об ошибке, если выполняется ответ модели или обрабатывается другая команда. Если нет полезного разговора или не удалось сгенерировать итоги, `/recap` показывает краткое информационное сообщение вместо итогов — ручная команда всегда что-то возвращает.

**Автоматический запуск при возвращении после отсутствия:**

Если терминал был свёрнут **5+ минут** и снова получает фокус, автоматически генерируются и показываются итоги (только когда не выполняется ответ модели; в противном случае ожидается завершение текущего ответа, а затем итоги запускаются). В отличие от ручной команды, автоматический запуск полностью молчалив при ошибке: если генерация не удалась или нечего обобщать, сообщение не добавляется в историю. Управляется параметром `general.showSessionRecap` (по умолчанию: `false`); ручная команда `/recap` всегда работает независимо от этого параметра.

**Пример:**

```
> /recap

❯ Рефакторинг loopDetectionService.ts для устранения OOM при длительных сессиях,
  вызванного неограниченным streamContentHistory и contentStats. Следующий шаг —
  реализовать вариант B (LRU скользящее окно с FNV-1a) после подтверждения.
```

> [!tip]
>
> Настройте быструю модель через `/model --fast <модель>` (например, `qwen3-coder-flash`), чтобы сделать `/recap` быстрым и дешёвым. Установите `general.showSessionRecap` в `true`, чтобы включить автоматический запуск; ручная команда `/recap` всегда работает независимо от этого параметра.

### 1.8 Просмотр изменений (`/diff`)

Команда `/diff` открывает интерактивный просмотр изменений, показывая незакоммиченные изменения и изменения по шагам. Используйте ←/→ для переключения между текущим git diff и отдельными шагами разговора, ↑/↓ для навигации по файлам и Enter для просмотра встроенного diff.

**Как это работает:**

В интерактивном режиме `/diff` открывает диалог с **выбором источника** в верхней части:

- **Current** — рабочее дерево vs HEAD (`git diff HEAD`). Показывает все незакоммиченные изменения, включая staged, unstaged и неотслеживаемые файлы.
- **T1, T2, T3, …** — изменения по шагам, по одной вкладке на каждый шаг модели, который изменял файлы. Самые последние шаги отображаются первыми. Каждая вкладка показывает превью оригинального промпта для контекста.

В списке файлов отображается статистика по каждому файлу (добавлено/удалено строк) с тегами для особых состояний (`new`, `deleted`, `untracked`, `binary`, `truncated`, `oversized`). Нажмите Enter на файле, чтобы просмотреть его встроенный diff с подсветкой синтаксиса.

Изменения по шагам требуют, чтобы сохранение контрольных точек файлов было включено (по умолчанию включено в интерактивном режиме). Если сохранение контрольных точек выключено, доступен только источник «Current».

**Горячие клавиши:**

| Клавиша       | Действие                                        |
| ------------- | ----------------------------------------------- |
| `←` / `→`     | Переключение между источниками (Current / T1 / T2…) |
| `↑` / `↓`     | Навигация по списку файлов                      |
| `j` / `k`     | Навигация по списку файлов (в стиле vim)        |
| Enter         | Просмотр встроенного diff для выбранного файла  |
| `←` / Esc     | Возврат к списку файлов из просмотра diff       |
| Esc           | Закрыть диалог                                  |

**Пример:**

```
┌ /diff · Шаг 3 "рефакторинг auth middleware" ──── 3 файла +45 -12 ┐
│                                                                     │
│ ◀ Current · T3 · T2 · T1 ▶                                         │
│                                                                     │
│ › src/utils/parser.ts                              +30 -8           │
│   src/utils/parser.test.ts                         +12 -2           │
│   README.md                                        +3 -2            │
│                                                                     │
│ ←/→ источник · ↑/↓ файл · Enter просмотр · Esc закрыть             │
└─────────────────────────────────────────────────────────────────────┘
```

**Неинтерактивный режим:**

В безголовом (`--prompt`) или неинтерактивном контексте `/diff` выводит текстовую сводку рабочего дерева vs HEAD. Навигация по шагам недоступна.

```
3 файла изменено, +45 / -12
  +30  -8  src/utils/parser.ts
  +12  -2  src/utils/parser.test.ts
   +3  -2  README.md
```

### 1.9 Информация, настройки и справка

Команды для получения информации и выполнения системных настроек.

| Команда           | Описание                                                                                                                          | Примеры использования                                                              |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `/help`           | Отображение справочной информации по доступным командам                                                                            | `/help` или `/?`                                                                    |
| `/status`         | Отображение информации о версии                                                                                                   | `/status` или `/about`                                                              |
| `/status paths`   | Отображение путей к файлам сессии и логам текущей сессии                                                                          | `/status paths`                                                                     |
| `/stats`          | Открыть интерактивную панель статистики использования (вкладки «Сессия», «Активность», «Эффективность»)                           | `/stats` или `/usage`                                                               |
| `/stats model`    | Показать разбивку токенов по моделям и примерную стоимость                                                                        | `/stats model`                                                                      |
| `/stats tools`    | Показать количество вызовов инструментов по каждому инструменту                                                                   | `/stats tools`                                                                      |
| `/stats skills`   | Показать количество вызовов навыков для текущей активной сессии (только в живом режиме; исключает межсессионную дневную/месячную активность) | `/stats skills`                                                                     |
| `/stats daily`    | Показать ежедневную статистику использования токенов                                                                              | `/stats daily` (псевдоним `day`), `/stats day [YYYY-MM-DD]`                         |
| `/stats monthly`  | Показать ежемесячную статистику использования токенов                                                                             | `/stats monthly` (псевдоним `month`), `/stats month [YYYY-MM]`                      |
| `/stats export`   | Экспорт статистики использования в CSV или JSON                                                                                   | `/stats export <daily|monthly> [date|month] [--format csv|json] [--output path]` |
| `/settings`       | Открыть редактор настроек                                                                                                         | `/settings`                                                                         |
| `/auth`           | Изменить способ аутентификации                                                                                                    | `/auth`, `/connect`, `/login`                                                       |
| `/doctor`         | Запуск диагностики установки и окружения                                                                                          | `/doctor`, `/doctor memory`                                                         |
| → `memory`        | Показать диагностику памяти текущего процесса                                                                                     | `/doctor memory [--json] [--sample] [--snapshot]`                                   |
| → `cpu-profile`   | Записать профиль CPU для анализа в Chrome DevTools                                                                                | `/doctor cpu-profile [--duration <секунды>]`                                        |
| → `rollback`      | Откатить автономный CLI-бинарник до предыдущей версии (только для автономных установок; для истории разговоров используйте `/rewind`) | `/doctor rollback`                                                                  |
| `/docs`           | Открыть полную документацию Qwen Code в браузере                                                                                  | `/docs`                                                                             |
| `/ide`            | Управление интеграцией с IDE                                                                                                      | `/ide status`, `/ide install`, `/ide enable`, `/ide disable`                        |
| `/insight`        | Сгенерировать инсайты по программированию из истории чата                                                                         | `/insight`                                                                          |
| `/setup-github`   | Настроить GitHub Actions                                                                                                          | `/setup-github`                                                                     |
| `/bug`            | Отправить сообщение об ошибке Qwen Code                                                                                           | `/bug Нажатие кнопки не отвечает`                                                    |
| `/copy`           | Скопировать в буфер обмена: ответ (N-ый с конца), код (по языку), LaTeX или Mermaid                                               | `/copy`, `/copy 2`, `/copy python`, `/copy latex`, `/copy mermaid`                  |
| `/quit`           | Немедленно выйти из Qwen Code                                                                                                     | `/quit` или `/exit`                                                                 |

> [!warning]
>
> Команда `/doctor memory --snapshot` создаёт снимок V8 heap, который может содержать промпты, содержимое файлов, ключи API и результаты инструментов текущей сессии. Перед отправкой проверьте файл.

### 1.10 Часто используемые сочетания клавиш

| Сочетание клавиш   | Действие                    | Примечание                                                                |
| ------------------ | --------------------------- | ------------------------------------------------------------------------- |
| `Ctrl/cmd+L`       | Очистить экран              | Очищает только видимую область (не сбрасывает сессию как `/clear`)        |
| `Ctrl/cmd+T`       | Показать/скрыть описание инструментов | Управление MCP-инструментами                                               |
| `Ctrl/cmd+C`×2     | Подтверждение выхода        | Безопасный механизм выхода                                                |
| `Ctrl/cmd+Z`       | Отменить ввод               | Редактирование текста                                                      |
| `Ctrl/cmd+Shift+Z` | Повторить ввод              | Редактирование текста                                                      |

### 1.11 Команды аутентификации

Используйте `/auth` внутри сессии Qwen Code для настройки аутентификации. Используйте `/doctor` для проверки текущего статуса аутентификации и окружения.

| Команда   | Описание                                                           |
| --------- | ------------------------------------------------------------------ |
| `/auth`   | Настроить аутентификацию в интерактивном режиме (псевдонимы: `/connect`, `/login`) |
| `/doctor` | Показать проверки аутентификации и окружения                      |

> [!note]
>
> Автономная CLI-команда `qwen auth` была удалена. Устаревшие вызовы, такие как `qwen auth status`, выводят уведомление об удалении с инструкциями по миграции. Полные сведения см. на странице [Аутентификация](../configuration/auth).

## 2. Команды @ (Добавление файлов)

Команды @ используются для быстрого добавления содержимого локального файла или каталога в диалог.

| Формат команды     | Описание                                              | Примеры                                          |
| ------------------ | ----------------------------------------------------- | ------------------------------------------------ |
| `@<путь к файлу>`  | Вставить содержимое указанного файла                  | `@src/main.py Объясни, пожалуйста, этот код`     |
| `@<путь к каталогу>`| Рекурсивно прочитать все текстовые файлы в каталоге   | `@docs/ Суммируй содержимое этого документа`     |
| Отдельный `@`      | Используется при обсуждении самого символа `@`        | `@ Для чего этот символ используется в программировании?` |

Примечание: пробелы в путях нужно экранировать обратной косой чертой (например, `@My\ Documents/file.txt`)

## 3. Восклицательные команды (`!`) — выполнение shell-команд

Восклицательные команды позволяют выполнять системные команды непосредственно в Qwen Code.

| Формат команды       | Описание                                                     | Примеры                               |
| -------------------- | ------------------------------------------------------------ | -------------------------------------- |
| `!<shell-команда>`   | Выполнить команду в под-оболочке                              | `!ls -la`, `!git status`               |
| Отдельный `!`        | Переключить режим оболочки: любой ввод выполняется как команда оболочки | `!`(Enter) → Ввод команды → `!`(выход) |

Переменные окружения: При выполнении команд через `!` устанавливается переменная окружения `QWEN_CODE=1`.

## 4. Пользовательские команды

Сохраняйте часто используемые промпты как команды быстрого доступа для повышения эффективности работы и обеспечения согласованности.

> [!note]
>
> Пользовательские команды теперь используют формат Markdown с опциональным YAML-фронтмэттером. Формат TOML устарел, но всё ещё поддерживается для обратной совместимости. При обнаружении файлов TOML будет отображаться приглашение к автоматической миграции.

### Краткий обзор

| Функция             | Описание                                      | Преимущества                             | Приоритет | Примеры сценариев использования                |
| ------------------- | --------------------------------------------- | ---------------------------------------- | --------- | ---------------------------------------------- |
| Пространство имён   | Подкаталог создаёт команды с двоеточием в имени | Лучшая организация команд                  |           |                                                |
| Глобальные команды  | `~/.qwen/commands/`                           | Доступны во всех проектах                | Низкий    | Личные часто используемые команды, кросс-проектное использование |
| Команды проекта     | `<корневая директория проекта>/.qwen/commands/`| Привязаны к проекту, поддаются контролю версий| Высокий   | Общие команды для команды, специфичные для проекта |

Правила приоритета: Команды проекта > Команды пользователя (если имена совпадают, используется команда проекта).

### Правила именования команд

#### Таблица соответствия пути файла и имени команды

| Расположение файла                       | Сгенерированная команда | Пример вызова            |
| ---------------------------------------- | ----------------------- | ------------------------ |
| `~/.qwen/commands/test.md`               | `/test`                 | `/test Параметр`         |
| `<проект>/.qwen/commands/git/commit.md`  | `/git:commit`           | `/git:commit Сообщение`  |

Правила именования: Разделители путей (`/` или `\`) преобразуются в двоеточие (`:`).

### Спецификация формата Markdown (рекомендуется)

Пользовательские команды используют файлы Markdown с опциональным YAML-фронтмэттером:

```markdown
---
description: Опциональное описание (отображается в /help)
---

Содержимое вашего промпта.
Используйте {{args}} для вставки параметров.
```

| Поле         | Обязательно | Описание                              | Пример                                    |
| ------------ | ----------- | ------------------------------------- | ----------------------------------------- |
| `description`| Нет         | Описание команды (отображается в /help)| `description: Инструмент анализа кода`     |
| Тело промпта | Да          | Содержимое промпта, отправляемое модели| Любое содержимое Markdown после фронтмэттера |

### Формат TOML (устаревший)

> [!warning]
>
> **Устарел:** Формат TOML всё ещё поддерживается, но будет удалён в будущей версии. Пожалуйста, мигрируйте на формат Markdown.

| Поле         | Обязательно | Описание                              | Пример                                    |
| ------------ | ----------- | ------------------------------------- | ----------------------------------------- |
| `prompt`     | Да          | Содержимое промпта, отправляемое модели| `prompt = "Пожалуйста, проанализируй код: {{args}}"` |
| `description`| Нет         | Описание команды (отображается в /help)| `description = "Инструмент анализа кода"` |

### Механизм обработки параметров

| Метод обработки               | Синтаксис           | Сценарии применения                  | Функции безопасности                      |
| ----------------------------- | ------------------- | ------------------------------------ | ----------------------------------------- |
| Контекстно-зависимая вставка  | `{{args}}`          | Когда нужен точный контроль параметров| Автоматическое экранирование для оболочки  |
| Обработка параметров по умолчанию | Без специальной маркировки | Простые команды, добавление параметров в конец | Добавление как есть                       |
| Вставка shell-команды         | `!{команда}`        | Когда нужно динамическое содержимое  | Требуется подтверждение перед выполнением |

#### 1. Контекстно-зависимая вставка (`{{args}}`)

| Сценарий         | Конфигурация TOML                             | Способ вызова          | Фактический эффект            |
| ---------------- | --------------------------------------------- | ---------------------- | ----------------------------- |
| Прямая вставка   | `prompt = "Исправить: {{args}}"`              | `/fix "Проблема с кнопкой"` | `Исправить: "Проблема с кнопкой"` |
| В shell-команде  | `prompt = "Поиск: !{grep {{args}} .}"`        | `/search "привет"`     | Выполнить `grep "привет" .`   |

#### 2. Обработка параметров по умолчанию

| Ситуация с вводом | Метод обработки                                            | Пример                                          |
| ----------------- | ---------------------------------------------------------- | ----------------------------------------------- |
| Есть параметры    | Добавить в конец промпта (разделяя двумя переводами строки)| `/cmd параметр` → Исходный промпт + параметр    |
| Нет параметров    | Отправить промпт как есть                                  | `/cmd` → Исходный промпт                        |
🚀 Динамическое внедрение контента

| Тип внедрения            | Синтаксис         | Порядок обработки    | Назначение                               |
| ------------------------ | ----------------- | -------------------- | ---------------------------------------- |
| Содержимое файла         | `@{file path}`    | Обрабатывается первым| Внедрение статических ссылочных файлов   |
| Команды оболочки         | `!{command}`      | Обрабатывается средним| Внедрение результатов динамического выполнения |
| Замена параметров        | `{{args}}`        | Обрабатывается последним| Внедрение пользовательских параметров    |

#### 3. Исполнение команд оболочки (`!{...}`)

| Операция                          | Взаимодействие с пользователем |
| --------------------------------- | ------------------------------ |
| 1. Разбор команды и параметров    | -                              |
| 2. Автоматическое экранирование оболочки | -                        |
| 3. Показать диалог подтверждения  | ✅ Подтверждение пользователя  |
| 4. Выполнить команду              | -                              |
| 5. Внедрить вывод в подсказку     | -                              |

Пример: Генерация сообщения коммита Git

````markdown
---
description: Generate Commit message based on staged changes
---

Please generate a Commit message based on the following diff:

```diff
!{git diff --staged}
```
````

#### 4. Внедрение содержимого файла (`@{...}`)

| Тип файла       | Статус поддержки          | Метод обработки                      |
| --------------- | ------------------------- | ------------------------------------ |
| Текстовые файлы | ✅ Полная поддержка       | Прямое внедрение содержимого         |
| Изображения/PDF | ✅ Мультимодальная поддержка| Кодировать и внедрять                |
| Бинарные файлы  | ⚠️ Ограниченная поддержка | Может быть пропущен или обрезан      |
| Директория      | ✅ Рекурсивное внедрение  | Следование правилам .gitignore       |

Пример: Команда для ревью кода

```markdown
---
description: Code review based on best practices
---

Review {{args}}, reference standards:

@{docs/code-standards.md}
```

### Практический пример создания

#### Таблица шагов создания команды «Рефакторинг чистой функции»

| Операция                        | Команда/Код                              |
| ------------------------------- | ---------------------------------------- |
| 1. Создать структуру директорий | `mkdir -p ~/.qwen/commands/refactor`     |
| 2. Создать файл команды         | `touch ~/.qwen/commands/refactor/pure.md`|
| 3. Редактировать содержимое команды| Обратитесь к полному коду ниже.         |
| 4. Проверить команду            | `@file.js` → `/refactor:pure`           |

```markdown
---
description: Refactor code to pure function
---

Please analyze code in current context, refactor to pure function.
Requirements:

1. Provide refactored code
2. Explain key changes and pure function characteristic implementation
3. Maintain function unchanged
```

### Сводка лучших практик для пользовательских команд

#### Таблица рекомендаций по проектированию команд

| Пункты практики      | Рекомендуемый подход                | Избегайте                                    |
| -------------------- | ----------------------------------- | -------------------------------------------- |
| Именование команд    | Используйте пространства имён для организации | Избегайте слишком общих имён          |
| Обработка параметров | Четко используйте `{{args}}`        | Полагаться на добавление по умолчанию         |
| Обработка ошибок     | Используйте вывод ошибок оболочки   | Игнорировать сбой выполнения                 |
| Организация файлов   | Организуйте по функциям в директориях | Все команды в корневой директории           |
| Поле описания        | Всегда предоставляйте четкое описание | Полагаться на автоматически сгенерированное описание |

#### Таблица напоминаний о функциях безопасности

| Механизм безопасности | Эффект защиты               | Действие пользователя       |
| --------------------- | --------------------------- | --------------------------- |
| Экранирование оболочки| Предотвращает внедрение команд | Автоматическая обработка  |
| Подтверждение выполнения| Избежать случайного выполнения | Подтверждение в диалоговом окне |
| Сообщение об ошибках | Помогает диагностировать проблемы | Просмотр информации об ошибках |

## 5. Подкоманды CLI

Эти команды запускаются из оболочки как `qwen <подкоманда>` перед началом интерактивного сеанса.

### Управление сеансами

| Команда               | Описание                               | Примеры использования                                      |
| --------------------- | -------------------------------------- | ------------------------------------------------------------ |
| `qwen sessions list`  | Список недавних сеансов беседы         | `qwen sessions list`, `qwen sessions list --json --limit 50` |

#### `qwen sessions list`

Выводит список ваших недавних сеансов Qwen Code с метаданными.

**Флаги:**

| Флаг      | Тип     | По умолчанию | Описание                                                         |
| --------- | ------- | ------------ | ---------------------------------------------------------------- |
| `--json`  | boolean | `false`      | Вывод в формате JSON Lines (один объект JSON на строку)          |
| `--limit` | number  | `20`         | Максимальное количество отображаемых сеансов                     |

**Вывод, удобочитаемый для человека (по умолчанию):**

Таблица со столбцами: SESSION ID, STARTED (метка времени UTC), TITLE, BRANCH, PROMPT.

**Вывод в JSON (`--json`):**

Выводит JSON Lines в stdout. Каждая строка представляет собой объект JSON с полями:

```
sessionId, startTime, mtime, prompt, gitBranch, customTitle, titleSource, filePath, cwd
```

Подсказка «есть ещё сеансы» выводится через stderr, чтобы передача в `jq` оставалась безопасной.

**Примеры:**

```bash
# Show last 20 sessions (default)
qwen sessions list

# Show last 50 sessions
qwen sessions list --limit 50

# Output as JSON for scripting
qwen sessions list --json | jq .
```