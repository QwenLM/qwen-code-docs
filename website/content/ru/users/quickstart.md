# Быстрый старт

> 👏 Добро пожаловать в Qwen Code!

Это руководство по быстрому запуску поможет вам начать использовать помощь ИИ при программировании всего за несколько минут. В итоге вы узнаете, как использовать Qwen Code для типичных задач разработки.

## Прежде чем начать

Убедитесь, что у вас есть:

- Открытый **терминал** или командная строка
- Проект с кодом, над которым можно поработать
- Аккаунт [Qwen Code](https://chat.qwen.ai/auth?mode=register)

## Шаг 1: Установите Qwen Code

Для установки Qwen Code используйте один из следующих способов:

### NPM (рекомендуется)

Требуется [Node.js 20+](https://nodejs.org/download), можно использовать `node -v`, чтобы проверить версию. Если он не установлен, используйте следующую команду для его установки.

Если у вас [установлен Node.js или более новая версия](https://nodejs.org/en/download/):

```sh
npm install -g @qwen-code/qwen-code@latest
```

### Homebrew (macOS, Linux)

```sh
brew install qwen-code
```

## Шаг 2: Войдите в свой аккаунт

Для использования Qwen Code требуется учетная запись. При запуске интерактивного сеанса с помощью команды `qwen` вам потребуется войти в систему:

```bash

# При первом использовании вас попросят войти в систему
qwen
```

```bash

# Следуйте инструкциям для входа в свою учетную запись
/auth
```

Выберите `Qwen OAuth`, войдите в свою учетную запись и следуйте инструкциям для подтверждения. После входа в систему ваши учетные данные сохраняются, и вам больше не нужно будет входить снова.

> [!note]
>
> Когда вы впервые аутентифицируете Qwen Code с помощью своей учетной записи Qwen, для вас автоматически создается рабочая область под названием ".qwen". Эта рабочая область обеспечивает централизованное отслеживание затрат и управление всеми расходами на использование Qwen Code в вашей организации.

> [!tip]
>
> Если вам нужно снова войти в систему или переключить учетные записи, используйте команду `/auth` внутри Qwen Code.

## Шаг 3: Начните свой первый сеанс

Откройте терминал в любой директории проекта и запустите Qwen Code:

```bash

# опционально
cd /path/to/your/project

# запуск qwen
qwen
```

Вы увидите приветственное окно Qwen Code с информацией о вашем сеансе, недавних разговорах и последних обновлениях. Введите `/help`, чтобы получить список доступных команд.

## Чат с Qwen Code

### Задайте свой первый вопрос

Qwen Code проанализирует ваши файлы и предоставит краткое изложение. Вы также можете задать более конкретные вопросы:

```
объясни структуру папок
```

Вы также можете спросить у Qwen Code о его собственных возможностях:

```
что может делать Qwen Code?
```

> [!note]
>
> Qwen Code читает ваши файлы по мере необходимости — вам не нужно вручную добавлять контекст. Qwen Code также имеет доступ к своей собственной документации и может отвечать на вопросы о своих функциях и возможностях.

### Внесите первые изменения в код

Теперь заставим Qwen Code выполнить реальное программирование. Попробуйте простую задачу:

```
добавь функцию hello world в основной файл
```

Qwen Code выполнит следующие действия:

1. Найдет подходящий файл
2. Покажет предложенные изменения
3. Запросит ваше одобрение
4. Выполнит редактирование

> [!note]
>
> Qwen Code всегда запрашивает разрешение перед изменением файлов. Вы можете одобрять отдельные изменения или включить режим "Принимать все" для сессии.

### Использование Git с Qwen Code

Qwen Code делает операции Git разговорными:

```
какие файлы я изменил?
```

```
закоммить мои изменения с описательным сообщением
```

Вы также можете запрашивать более сложные операции Git:

```
создай новую ветку под названием feature/quickstart
```

```
покажи мне последние 5 коммитов
```

```
помоги мне решить конфликты слияния
```

### Исправление ошибки или добавление функции

Qwen Code отлично справляется с отладкой и реализацией новых возможностей.

Опишите, чего вы хотите, на естественном языке:

```
добавить проверку ввода в форму регистрации пользователя
```

Или исправьте существующие проблемы:

```
есть ошибка, из-за которой пользователи могут отправлять пустые формы — исправьте это
```

Qwen Code выполнит следующее:

- Найдет соответствующий код
- Поймет контекст
- Реализует решение
- Запустит тесты, если они доступны

### Попробуйте другие распространенные рабочие процессы

Существует множество способов работы с Qwen Code:

**Рефакторинг кода**

```
рефакторить модуль аутентификации для использования async/await вместо коллбэков
```

**Написание тестов**

```
написать модульные тесты для функций калькулятора
```

**Обновление документации**

```
обновить README с инструкциями по установке
```

**Код-ревью**

```
проверить мои изменения и предложить улучшения
```

> [!tip]
>
> **Помните**: Qwen Code — это ваш ИИ-партнер-программист. Общайтесь с ним так же, как с полезным коллегой — опишите, чего вы хотите достичь, и он поможет вам в этом.

## Основные команды

Вот самые важные команды для ежедневного использования:

| Команда               | Что делает                                       | Пример                        |
| --------------------- | ------------------------------------------------ | ----------------------------- |
| `qwen`                | запустить Qwen Code                              | `qwen`                        |
| `/auth`               | изменить метод аутентификации                    | `/auth`                       |
| `/help`               | отобразить справочную информацию по доступным командам | `/help` или `/?`              |
| `/compress`           | заменить историю чата на сводку для экономии токенов | `/compress`                   |
| `/clear`              | очистить содержимое экрана терминала              | `/clear` (горячая клавиша: `Ctrl+L`) |
| `/theme`              | изменить визуальную тему Qwen Code               | `/theme`                      |
| `/language`           | просмотреть или изменить языковые настройки      | `/language`                   |
| → `ui [язык]`         | установить язык интерфейса пользователя          | `/language ui ru-RU`          |
| → `output [язык]`     | установить язык вывода LLM                       | `/language output Russian`    |
| `/quit`               | немедленно выйти из Qwen Code                    | `/quit` или `/exit`           |

Полный список команд см. в [справочнике CLI](./features/commands).

## Советы для начинающих

**Будьте конкретны в своих запросах**

- Вместо: "исправь баг"
- Попробуйте: "исправь баг при входе, из-за которого пользователи видят пустой экран после ввода неправильных учетных данных"

**Используйте пошаговые инструкции**

- Разбивайте сложные задачи на шаги:

```
1. создай новую таблицу базы данных для профилей пользователей
2. создай конечную точку API для получения и обновления профилей пользователей
3. создай веб-страницу, которая позволяет пользователям просматривать и редактировать свою информацию
```

**Позвольте Qwen Code исследовать сначала**

- Прежде чем вносить изменения, дайте Qwen Code понять ваш код:

```
проанализируй схему базы данных
```

```
создай панель мониторинга, показывающую продукты, которые чаще всего возвращаются нашими британскими клиентами
```

**Экономьте время с помощью ярлыков**

- Нажмите `?`, чтобы увидеть все доступные сочетания клавиш
- Используйте Tab для автозаполнения команд
- Нажмите ↑ для истории команд
- Введите `/`, чтобы увидеть все команды с косой чертой

## Получение справки

- **В Qwen Code**: Введите `/help` или спросите "как я могу..."
- **Документация**: Вы уже здесь! Просмотрите другие руководства
- **Сообщество**: Присоединяйтесь к нашему [обсуждению на GitHub](https://github.com/QwenLM/qwen-code/discussions) для получения советов и поддержки